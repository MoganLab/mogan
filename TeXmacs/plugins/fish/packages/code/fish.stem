(document (TeXmacs "2.1.4")
  (style (tuple "source" "std"))
  (body (document (active* (src-title (document (compound "src-package" "fish" "1.0")
                                        (src-purpose (document "FISH Language support for TeXmacs"))
                                      ) ;document
                           ) ;src-title
                  ) ;active*
          (concat (use-module "(fish fish-format)") " " (use-module "(fish fish-edit)"))
          (assign "fish"
            (macro "body"
              (with "mode" "prog" "prog-language" "fish" "font-family" "rm" (arg "body"))
            ) ;macro
          ) ;assign
          (assign "fish-code"
            (macro "body" (document (pseudo-code (document (fish (arg "body"))))))
          ) ;assign
        ) ;document
  ) ;body
  (initial (collection (associate "preamble" "true") (associate "sfactor" "5")))
) ;document
