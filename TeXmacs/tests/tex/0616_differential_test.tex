\documentclass{article}
\begin{document}

Here is a LaTeX math test for differentials:

\( dx + dy = dz \)

\[ dr = d\rho d\theta \]

Inline math: $x dx$ and $\sin x dx$.

Greek variants: $d\varrho + d\vartheta$.

Additional tests for u, v, w differentials:

\( du + dv = dw \)

Inline math: $u du$ and $\cos v dv$ and $w dw$.

Greek variants of u, v, w: $d\upsilon + d\phi + d\varphi + d\omega$.

\end{document}
