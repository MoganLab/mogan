name: Clang-Format Check on Debian 13

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "!src/Plugins/Macos/**"
      - "!src/Plugins/Windows/**"
      - "tests/**"
      - "moebius/**"
      - "3rdparty/lolly/**"
      - "TeXmacs/progs/**"
      - "TeXmacs/plugins/**"
      - "tools/format/liii/**"
      - ".clang-format"
      - "gfexclude.json"
      - "gfproject.json"
      - ".github/workflows/ci-format-check-debian13.yml"
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "!src/Plugins/Macos/**"
      - "!src/Plugins/Windows/**"
      - "tests/**"
      - "moebius/**"
      - "3rdparty/lolly/**"
      - "TeXmacs/progs/**"
      - "TeXmacs/plugins/**"
      - "tools/format/liii/**"
      - ".clang-format"
      - "gfexclude.json"
      - "gfproject.json"
      - ".github/workflows/ci-format-check-debian13.yml"
  workflow_dispatch:

jobs:
  clang-format-check:
    container: debian:13
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
    steps:
      - name: Install dependencies
        run: |
          DEBIAN_FRONTEND=noninteractive apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y clang-format-19 curl ca-certificates

      - uses: actions/checkout@v3
        with:
          fetch-depth: 1

      - name: Download & install goldfish
        run: |
          curl -fsSL -o /tmp/gf.deb https://github.com/MoganLab/goldfish/releases/download/v18.11.11/goldfish-scheme-x86_64-v18.11.11.deb
          dpkg -i /tmp/gf.deb
          gf version

      - name: Run gf format --check (C++ + Scheme)
        run: gf format --check
