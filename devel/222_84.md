# [222_84] 极简模式：隐藏菜单栏、模式工具栏、焦点工具栏

## 相关文档
- [x_y.md](x_y.md) - 开发文档模板

## 任务相关的代码文件
- `TeXmacs/progs/texmacs/menus/view-menu.scm` - 查看菜单定义
- `TeXmacs/progs/texmacs/texmacs/tm-view.scm` - 视图状态切换函数
- `TeXmacs/progs/kernel/texmacs/tm-modes.scm` - 模式状态定义
- `TeXmacs/progs/texmacs/menus/main-menu.scm` - 右键菜单定义
- `src/Texmacs/Window/tm_frame.cpp` - C++ 层工具栏显示控制
- `src/Plugins/Qt/qt_tm_widget.cpp` - Qt 层 UI 可见性控制

## 如何测试
1. 启动 Mogan
2. 点击 `查看` 菜单，确认有 `极简模式` 选项
3. 点击 `极简模式`，确认菜单栏、模式工具栏、焦点工具栏都被隐藏
4. 在编辑区域右键，确认有 `极简模式` 选项可退出
5. 退出极简模式后，确认之前被隐藏的 UI 元素恢复显示

## 如何提交
```bash
# 提交前执行的最少测试步骤
xmake build mogan
./build/mogan
```

## 2026/05/08 第一步：定义极简模式状态与切换函数

### What
在 Scheme 层定义极简模式的状态判断和切换函数。

### Why
参考专注模式（`toggle-focus-mode`）的实现方式，极简模式需要：
1. 一个状态变量/函数判断当前是否处于极简模式
2. 一个切换函数进入/退出极简模式
3. 保存进入极简模式前的 UI 状态，以便退出时恢复

### How
1. 在 `tm-modes.scm` 中定义 `simplest-mode?`：
   - 极简模式状态 = 菜单栏隐藏 且 模式工具栏隐藏 且 焦点工具栏隐藏
   - 即 `(simplest-mode% (and (not (visible-header?)) (not (visible-icon-bar? 1)) (not (visible-icon-bar? 2))))`

2. 在 `tm-view.scm` 中实现 `toggle-simplest-mode`：
   - 需要保存进入极简模式前的状态（菜单栏、模式工具栏、焦点工具栏的可见性）
   - 使用 `saved-simplest-state` 变量存储三元组 `(header-visible? mode-visible? focus-visible?)`
   - 进入极简模式时：
     - 保存当前状态
     - 调用 `(show-header #f)`、`(show-icon-bar 1 #f)`、`(show-icon-bar 2 #f)` 隐藏三个 UI 元素
   - 退出极简模式时：
     - 根据保存的状态恢复三个 UI 元素

## 2026/05/08 第二步：在查看菜单和右键菜单中添加入口

### What
在查看菜单中添加 `极简模式` 选项，并在右键菜单中添加退出入口。

### Why
参考专注模式的交互方式：
- 专注模式在 `查看` 菜单中有 `Focus mode` 入口
- 进入专注模式后，右键菜单会出现 `Focus mode` 选项用于退出

### How
1. 在 `view-menu.scm` 的 `view-menu` 中，在 `Focus mode` 下方添加 `极简模式` 菜单项：
   ```scheme
   ("极简模式" (toggle-simplest-mode))
   ```

2. 在 `main-menu.scm` 的 `texmacs-popup-menu` 中，参照 `focus-popup-menu` 的实现，添加 `simplest-popup-menu`：
   ```scheme
   (menu-bind simplest-popup-menu
     ("极简模式" (toggle-simplest-mode)))

   (tm-menu (texmacs-popup-menu)
     (:require (simplest-mode?))
     (link simplest-popup-menu)
     ---
     (former))
   ```
   注意：需要处理与 `focus-popup-menu` 的共存逻辑，确保两个模式都能正确显示右键菜单。

## 2026/05/08 第三步：处理模式共存与边界情况

### What
处理极简模式与专注模式、全屏模式等其他模式的共存逻辑。

### Why
如果用户同时开启了专注模式和极简模式，退出其中一个时可能会意外恢复 UI 元素。

### How
- 由于专注模式只控制菜单栏，极简模式控制菜单栏+模式工具栏+焦点工具栏：
  - 进入极简模式时，如果专注模式已开启，保存的状态中菜单栏已经是隐藏的
  - 退出极简模式时，根据保存的状态恢复，不会与专注模式冲突
  - 但最好避免两个模式同时开启导致交互混乱：
    - 进入极简模式时，如果专注模式已开启，先退出专注模式（或直接覆盖）
    - 或者让极简模式的优先级高于专注模式

- 更简单的方案：极简模式和专注模式互斥，进入极简模式时自动退出专注模式，进入专注模式时自动退出极简模式。这是因为两者都控制菜单栏的显示/隐藏。
