# [1309] 使用 QML 重新实现插入参考文献（Bibliography Inserter）

## 1 相关文档
- [ai-docs/qml/README.md](../ai-docs/qml/README.md) — QML 弹窗体系与规范（DialogShell + atoms + run_qml_dialog）
- [devel/1308.md](1308.md) — 参考文献预览与 1308.bib 解析修复

## 2 任务相关的代码文件
- `src/Plugins/Qt/qml/Bibliography.qml` — 「插入/修改参考文献」成品 QML 弹窗
- `src/Plugins/Qt/BibliographyDialogBridge.hpp` — 参考文献对话框 C++↔QML bridge 声明
- `src/Plugins/Qt/BibliographyDialogBridge.cpp` — 参考文献对话框 bridge 实现（文件浏览、相对路径转换、光栅化预览与有效性校验）
- `src/Plugins/Qt/QTMQmlDialog.hpp` — `cpp_bibliography_dialog` 声明
- `src/Plugins/Qt/QTMQmlDialog.cpp` — `cpp_bibliography_dialog` 实现（run_qml_dialog + 测试钩子 `MOGAN_TEST_BIBLIOGRAPHY`）
- `src/Plugins/Qt/moganqml.qrc` + `src/Plugins/Qt/qml/qmldir` — 注册 Bibliography.qml
- `src/Scheme/L5/glue_qt.lua` — 注册 `cpp-bibliography-dialog` ↔ `cpp_bibliography_dialog`（mogan 唯一的 glue 声明源）
- `TeXmacs/plugins/lang/dic/en_US/zh_CN.scm` — 增加「此处预览参考文献的格式」「文件不存在」「无效的 BibTeX 文件」词条翻译
- `TeXmacs/plugins/latex/progs/latex/bibtex-bib-widgets.scm` — Scheme 侧增加 `(bib-has-entries?)`、`(bibliography-preview)` 光栅化预览校验与重构 `(open-bibliography-inserter)`
- `tests/Plugins/Qt/qml_load_test.cpp` — `test_bibliography_loads` QML 加载断言
- `TeXmacs/tests/python/1309.py` — 基于 pynput 的端到端自动化 UI 测试
- `TeXmacs/tests/bib/1308.bib` — 测试参考文献文件
- `ai-docs/qml/README.md` — 登记 Bibliography 成品弹窗与 bridge

## 3 如何测试

### 3.1 QML 单元测试（确定性加载）
```bash
xmake b qml_load_test && xmake r qml_load_test
```

### 3.2 自动化 UI 测试（pynput 端到端）
```bash
python3 TeXmacs/tests/python/1309.py
```
测试覆盖全流程：
1. 启动 Mogan STEM
2. 创建新标签页 (Ctrl+T)
3. 点击菜单「插入 -> 自动 -> 参考文献」弹出全新的 QML 参考文献对话框
4. 验证初始状态预览区提示「此处预览参考文献的格式」，主按钮「插入」处于置灰禁用状态
5. 键入不存在或无效的路径，验证文件输入框下方立即出现醒目红色错误提示（「文件不存在」/「无效的 BibTeX 文件」），主按钮保持禁用
6. 点击「浏览」按钮选择 `TeXmacs/tests/bib/1308.bib`
7. 验证错误提示自动清除，预览区以适度放大、清晰优雅的字号实时渲染出文献条目，主按钮激活
8. 在同行的功能栏中切换样式下拉框（如 `tm-alpha`），验证预览区条目标号与格式实时刷新
9. 点击「插入」主按钮，验证对话框关闭且参考文献成功插入文档，进程稳定不崩。

### 3.3 手动测试
启动 Mogan STEM，在文档中点击顶部菜单「插入 -> 自动 -> 参考文献」：
- 弹出统一现代风格的 QML 对话框，自动跟随浅色/深色主题；
- 未选择文件时，预览区提示「此处预览参考文献的格式」；
- 填入不存在的文件时，输入框下方出现红色「文件不存在」提示，主按钮置灰禁用；
- 点击「浏览」按钮选择 `.bib` 文件，预览区即时清晰呈现排版结果，主按钮点亮；
- 第二行中「样式」、「更新缓冲区」两个功能并排舒展，文字与开关对齐美观；
- 移除原先需要用户手动选择的「使用相对路径」开关，系统策略为「能用相对路径就使用相对路径，不能的时候使用绝对路径」；浏览选择文献后自动转为相对路径（跨盘符或未保存文档时保持绝对路径）；
- 切换样式下拉列表（如 `tm-plain`、`tm-alpha` 等），预览即时刷新；
- 点击「插入」完成文献插入。如果当前文档已有参考文献，再次打开会自动进入「修改参考文献」模式，预填已有文件和样式，主按钮对应为「修改」。

## 4 如何提交
```bash
gf fmt --changed-since=main
```

## 5 What
1. 使用项目统一的 QML 弹窗体系（`DialogShell` + `atoms` + `run_qml_dialog`）重新实现「插入 -> 自动 -> 参考文献」功能（同时支持已有文献时的「修改参考文献」）。
2. 移除冗余的手动「使用相对路径」开关选项，贯彻「能用相对路径就使用相对路径，不能的时候使用绝对路径」策略（选择文件自动相对化，无法相对时回退绝对路径），第二行简化为「样式」与「更新缓冲区」两栏并排，比例舒展。
3. 替换旧版生硬文案，未选文件时提示「此处预览参考文献的格式」；当文件路径非法或不存在时，在输入框下方及预览区直接展示明确的错误提示（「文件不存在」/「无效的 BibTeX 文件」），并置灰禁用主按钮防止误插入。
4. 解决旧版预览字号过小、阅读局促的问题，光栅化时使用适度放大倍率（`magnification: "1.05"`）与自适应容器宽，条目字号与标号比例协调。
5. 编写 `TeXmacs/tests/python/1309.py` 端到端 UI 自动化测试脚本，基于 pynput 驱动真实 GUI 完成无死角验证。

## 6 Why
1. 原参考文献对话框采用遗留的 Scheme `dialogue-window` / `tm-widget`，排版引擎自绘且控件简陋，无法适配现代主题。
2. 原预览区域文字字号极小，阅读体验差。
3. 原旧版缺少即时错误反馈，非法文件无法在界面上直观感知。
4. 纳入项目统一的 QML 模态弹窗规范，实现一次性提交、清晰的组件分层、键盘操作与深浅主题自适应。

## 7 How
1. **QML 前端 (`Bibliography.qml`)**：
   - 基于 `DialogShell` 构建，第一行放置标签 + 弹性拉伸输入框 + 右侧「浏览」按钮；文件输入框下方放置动态错误提示文本；
   - 第二行水平放置功能项：左侧「样式」下拉框（`EnumCombo`）、右侧「更新缓冲区」开关（`Toggle`），移除了由系统自动决策的「使用相对路径」开关；
   - 底部放置卡片边框预览区（`PreviewPane`）与操作按钮（`DialogButtons`，`primaryEnabled: root.previewStatus === "valid"`）。
2. **C++ 桥接与接口 (`BibliographyDialogBridge` / `cpp_bibliography_dialog`)**：
   - `BibliographyDialogBridge` 提供 `browse()`（`QFileDialog::getOpenFileName`）、`toRelativePath()` 与 `requestPreview(file, style)`（返回包含状态、提示与预览图片 data URL 的 `QVariantMap`）。选定文件后自动调用 `toRelativePath` 贯彻策略。
   - `cpp_bibliography_dialog` 经 `run_qml_dialog` 启动模态弹窗，注入多语言标签，提供 `MOGAN_TEST_BIBLIOGRAPHY` 钩子。
3. **Scheme 逻辑与校验 (`bibtex-bib-widgets.scm`)**：
   - 新增 `(bib-has-entries? st)`，精准检查是否包含有效的 `'bib-entry` 语法树节点。
   - 更新 `(bibliography-preview file-str style-str)`，分流空路径、不存在路径、无有效条目与有效文献四种状态，排版时使用适度放大 `"magnification" "1.05"`，经 `(cpp-rasterize-widget ...)` 生成清晰的预览图片并返回状态元组。
   - 更新 `(bibwid-set-url u)` 与 `(open-bibliography-inserter)`：统一按「能相对则相对，不能则绝对」解析存储路径。
4. **测试覆盖**：
   - `tests/Plugins/Qt/qml_load_test.cpp` 中新增 `test_bibliography_loads`。
   - `TeXmacs/tests/python/1309.py` 完整覆盖真实 GUI 下的初始状态断言、非法路径红色错误反馈、浏览选文件、放大预览检验、样式切换及插入文档全流程。

## 8 代码质量清理（/simplify 复审）

1. **复用**：
   - 文件输入行改用既有 `InputField` 原子（`label` + `actionLabel`/`actionClicked`），删除 ~50 行手写 Row/Rectangle/TextInput/MiniButton，同时继承其已解决的「外部改值刷新显示」契约；
   - `kv_map_to_tree` 增加 `utf8` 参数，`cpp_bibliography_dialog` 复用之，删除内联转换循环；
   - `BibliographyDialogBridge` 抽本地 `tmscm_to_qstring` 收敛 3 处裸 `utf8_to_qstring(cork_to_utf8(tmscm_to_string(...)))`；
   - `bib-has-entries?` 手写 named-let 遍历改为既有 `list-filter` 惯用法。
2. **简化**：
   - 还原对遗留死文件 `TeXmacs/progs/prog/glue-symbols.scm` 的无效编辑（mogan glue 声明在 `src/Scheme/L5/glue_qt.lua`）；
   - 删除 C++ 侧硬编码的 11 项样式回退列表（scheme 恒传非空 `styles`，回退不可达且会与 `bib-standard-styles` 漂移）；
   - 删除零调用的 `toAbsolutePath`（含测试 stub）、只写不读的 `bibwid-set-relative`、未使用的可变参数 `. opt-rel`；
   - 删除可由 `previewStatus` 派生的 `isValidBib` 状态与 bridge 返回map 里的 `valid` 键；
   - 非 Qt 回退分支复用 `let*` 已算好的 `modify?/init-url/init-style`，两个 `with msg` 块合一。
3. **效率**：文件路径键入逐字符触发 `requestPreview`（同步读文件 + parse-bib + 排版光栅化），QML 侧加 350ms 防抖 `Timer`，仅停笔后刷新预览。
4. **修正不一致**：QML 空预览占位串回退值由中文改为英文（与 C++ `qt_translate` 源串一致；实际显示恒由 C++ 注入的译文决定）。

## 9 预览区域从图片改为 tmfs 缓冲区（tmfs://aux/bib-preview）

### 9.1 What
1. 参考文献插入/修改工具的预览区域从静态光栅化图片（PNG base64 data URL）改为真实的 `tmfs` 辅助缓冲区（`tmfs://aux/bib-preview`）。
2. 在 QML 弹窗（`Bibliography.qml`）中移除 `PreviewPane` 位图展示，替换为 QQuickItem 占位容器；宿主在预览区覆盖嵌入原生只读 `tmfs` 编辑器视图。
3. 样式下拉框展开时联动隐藏原生预览视图，收起时恢复，避免原生 QWidget 与 QML 浮层层级冲突。

### 9.2 Why
1. 位图预览为静态栅格图，无法选中文本、无法复制文献内容、放大失真，且不具备原生滚动支持。
2. 改为真实的 tmfs 缓冲区后，复用 Mogan 核心排版引擎的实时渲染、原生矢量文字、字体缩放、文本选中复制与平滑滚动。

### 9.3 How
1. **Scheme 侧 (`bibtex-bib-widgets.scm`)**：
   - 参考 `0ebefefe4` 前的实现，恢复/提炼将 bib 格式化为 TeXmacs 树的函数 `(bib-to-tree file-str style-str)`；
   - 检验文件存在性与 `(bib-has-entries?)`，合法时调用 `(bibwid-output-content ...)` 生成 `(document (bib-list ...))` 排版树并返回状态元组。
2. **C++ 宿主与桥接 (`QTMQmlDialog.cpp` / `BibliographyDialogBridge.*`)**：
   - `cpp_bibliography_dialog` 中创建基于 `tmfs://aux/bib-preview` 的嵌入式视图 `texmacs_input_widget`，取出其原生 `QWidget*`；
   - 安装只读事件过滤器 `BibPreviewReadonlyFilter`，放行方向键与复制/全选快捷键，拦截文字输入；
   - 在 QML 场景就绪后通过 `post_setup` 回调将预览 QWidget 挂接并对齐到 QML 占位区域；
   - `requestPreview` 中经 Scheme 解析获取排版树，调用 `set_buffer_tree` 实时刷新该 tmfs 缓冲区；合法时显示控件，非法/空时隐藏控件由 QML 呈现提示；
   - 对话框关闭时安全销毁控件并清理 `tmfs://aux/bib-preview` 缓冲区；
   - 扩展 `is_aux_bib_buffer`，使菜单更新跳过该辅助缓冲区。
3. **QML 前端 (`Bibliography.qml`)**：
   - 移除 `PreviewPane` 与 `previewDataUrl`，保留边框卡片与非合法状态下的矢量文字提示；
   - 增加 `previewPlaceholder` 占位 Item，并在 `isComboOpen` 变化时调用 `bibBridge.setPreviewVisible` 切换可见性。

