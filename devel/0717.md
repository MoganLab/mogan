# [0717] 给 socket_link, socket_server 添加缺失的 tm_debug 头文件
## What
不def QTTEXMACS 情况下，texmacs 使用自己实现的 socket_server 作 server。

目前 socket_server.cpp 中用到了 tm_debug 中的输出函数，但没有 include tm_debug.hpp，应该是后期处理 debug 输出函数的时候统一作处理了，但是因为 Qt 版构建不涉及这块代码，没有发现此问题。

因此添加 include "tm_debug.hpp"
