# [0235] Chat tab 消息区域只读化

## 1 相关文档
- [0101.md](0101.md) - 任务文档模板

## 2 任务相关的代码文件
- src/Plugins/Qt/qt_chat_tab_widget.cpp

## 3 如何测试

### 3.1 确定性测试（单元测试）
无单元测试，需手动测试。

### 3.2 非确定性测试（文档验证）
```
xmake b stem
```

## 4 如何提交
```bash
xmake b stem
```

## 5 What
让 chat-tab 的消息区域（message buffer）变为只读，用户无法编辑消息内容，但可以复制和滚动。

1. 在 message widget 的 QTMWidget 上设置 `chat_message_readonly` 属性并安装 event filter
2. 在 eventFilter 中拦截普通键盘输入和 IME 输入，放行 Ctrl/⌘ 快捷键（复制、搜索、全选等）

## 6 Why
消息区域展示的是历史对话内容，用户不应编辑它。但需要保留鼠标选择复制、滚动浏览，以及 Ctrl+C/⌘+C 复制、Ctrl+F/⌘+F 搜索等快捷键功能。

## 7 How
通过 Qt event filter 机制实现：

1. 创建 message widget 后，找到其内部的 `QTMWidget` 子控件，设置 `chat_message_readonly` 属性标记，并安装 event filter
2. 在 `ChatConversationPanel::eventFilter` 中，对标记为 `chat_message_readonly` 的控件：
   - 拦截 `QEvent::InputMethod`（阻止 IME 输入）
   - 拦截无修饰键的 `QEvent::KeyPress`/`KeyRelease`（阻止普通文字输入）
   - 放行带 `Qt::ControlModifier` 或 `Qt::MetaModifier` 的按键（保留 Ctrl/⌘ 快捷键）
3. 跨平台适配：Windows/Linux 用 Ctrl，macOS 用 ⌘（Command），两者分别映射为 `Qt::ControlModifier` 和 `Qt::MetaModifier`
