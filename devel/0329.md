# [0329] Chat 输入区魔法粘贴后高度自适应

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [0302.md](0302.md) - LLM 聊天页实现

## 2 任务相关的代码文件
- `src/Plugins/Qt/qt_chat_tab_widget.hpp`
- `src/Plugins/Qt/qt_chat_tab_widget.cpp`

## 3 如何测试

### 3.1 确定性测试（单元测试）
```bash
xmake b qt_chat_tab_widget_test
QT_QPA_PLATFORM=offscreen xmake r qt_chat_tab_widget_test
```

### 3.2 非确定性测试（文档验证）
```text
1. 打开 AI Chat 页，聚焦输入区。
2. 复制一段包含多行数学公式的内容。
3. 触发 Ctrl+Shift+V，确认输入区高度可以立即随内容增长。
4. 右键输入区，选择右键菜单中的“魔法粘贴”，确认输入区高度同样会自动增长。
5. 发送消息或清空输入区，确认高度可以回落，不会只增不减。
```

## 4 如何提交

提交前执行以下最少步骤：

```bash
xmake b qt_chat_tab_widget_test
QT_QPA_PLATFORM=offscreen xmake r qt_chat_tab_widget_test
```

## 5 What
修复 Chat 输入区在右键菜单“魔法粘贴”后不自适应高度的问题，并保留当前较稳妥的轮询兜底方案。

1. 确认 `Ctrl+Shift+V` 和右键“魔法粘贴”走的是同一套底层插入逻辑，区别在于前者会经过输入框键盘事件，后者不会。
2. 在 `ChatConversationPanel` 中增加输入文档变化轮询，比较 `readInputMessage()` 与 `lastInputBody_`，内容变化后重新调节高度。
3. 在现有 `KeyRelease`、`InputMethod`、`Drop` 之外，补了 `FocusIn` 后的高度刷新，减少切回输入框时的滞后。
4. 调研了 editor/tree 层的稳定完成点，确认其存在，但当前未将它接入 Chat QWidget。

## 6 Why
问题不在“数学公式不能排版”，而在“右键菜单路径没有触发输入区当前绑定的高度刷新事件”。

- 原始实现只在 `KeyRelease`、`InputMethod`、`Drop` 后调用 `adjust_input_height()`。
- `Ctrl+Shift+V` 会走 `QTMWidget::keyPressEvent/keyReleaseEvent`，因此最终能触发高度更新。
- 右键菜单“魔法粘贴”通过菜单 action 执行命令，最终在 `edit_select.cpp` 中修改文档 tree，但不会回到输入框的 `KeyRelease` 路径。
- 数学公式内容常被组织成单个 `CONCAT` 子树，`DOCUMENT` 顶层子节点数可能仍为 1，因此单靠 `N(body)` 无法正确反映视觉行数。

## 7 How

### 7.1 问题定位

- 快捷键路径：
  - `src/Plugins/Qt/QTMWidget.cpp`
  - `keyPressEvent/keyReleaseEvent -> the_gui->process_keypress(...)`
- 右键菜单路径：
  - `src/Plugins/Qt/qt_ui_element.cpp`
  - `src/Plugins/Qt/QTMMenuHelper.cpp`
  - `src/Edit/Replace/edit_select.cpp`
- 这两条路径最终都会修改输入 buffer，但只有快捷键路径天然带有输入框事件。

### 7.2 失败的尝试

1. 直接用 `N(body)` 统计文档行数：
   - 纯文本多行有效。
   - 多行数学公式被包进单个 `CONCAT` 后失效。

2. 递归遍历 tree 统计“复合节点行数”：
   - `CONCAT` 内部多个行内元素会被误判成多行。
   - 一个公式可能被夸大到几十行。

3. 用 viewport/extents 反推内容高度：
   - 排版高度会受到当前 viewport 大小影响。
   - `setFixedHeight()` 撑大容器后，测量值也跟着变大，形成正反馈。

### 7.3 当前采用的方案

- 仍然使用 `edit_interface_rep::get_total_height(false)` 取得排版后的纯内容高度。
- 额外加一个 100ms `QTimer`：
  - 只要 `readInputMessage()` 结果和 `lastInputBody_` 不同，就调用 `schedule_input_height_adjust()`。
  - 这样菜单粘贴、脚本写入、异步输入恢复等非键盘路径也能触发高度更新。
- `schedule_input_height_adjust()` 继续用 `QTimer::singleShot(0, ...)` 推迟到当前事件循环末尾执行，避免读到旧排版结果。

### 7.4 关于“稳定完成点”的调研结论

底层其实存在更稳定的 tree 修改完成点：

- `edit_observer_rep::done()`
- `edit_done(...)`
- `editor_rep::post_notify(...)`

另外，tree observer 机制本身也支持 Scheme 回调：

- `scheme_observer(...)`
- `tree_pointer_rep::done(...)`

但当前 Chat 输入框还没有直接订阅这类完成通知的桥。要彻底替代轮询，需要同时解决两件事：

1. 把输入文档根节点的 `done` 事件桥接到 Qt 层。
2. 处理 `buffer-set-body` 这类“整棵根节点被替换”后的 observer 重挂问题。

在这两点都没做完整之前，轮询方案更稳妥，风险也更低。
