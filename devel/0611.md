# [0611] 修复 TikZ 插件图片显示及空 bounding box 处理

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [1013.md](1013.md) - TikZ 插件初始实现任务

## 2 任务相关的代码文件
- `TeXmacs/plugins/tikz/progs/init-tikz.scm`
- `TeXmacs/plugins/tikz/progs/binary/tikz.scm`
- `TeXmacs/plugins/tikz/goldfish/tm-tikz.scm`
- `TeXmacs/plugins/tikz/tests/tm-tikz-test.scm`

## 3 如何测试

### 3.1 确定性测试（单元测试）
1. 构建 Goldfish Scheme 解释器：
   ```bash
   xmake b goldfish
   ```
2. 运行 TikZ 插件单元测试：
   ```bash
   cd TeXmacs/plugins/tikz
   ../../../bin/goldfish load tests/tm-tikz-test.scm
   ```

### 3.2 非确定性测试（文档验证）
1. 构建 Mogan STEM：
   ```bash
   xmake b stem
   ```
2. 启动 Mogan STEM
3. 通过菜单 `Insert -> Session -> TikZ` 插入 TikZ 会话
4. 输入 `\draw (0,0) -- (1,1);`，验证是否能正常生成图片
5. 输入 `\draw (0,0);`，验证是否输出提示而不是报错或弹窗
6. 输入 `% -width 0.5par -height 0.5par` 魔术行，验证图片尺寸参数生效
7. 输入完整 LaTeX 文档（以 `\documentclass` 开头），验证直通模式

## 4 如何提交

提交前执行以下最少步骤：

```bash
xmake b stem
```

## 5 What

1. 修复 Mogan 无法显示 TikZ 生成图片的问题
2. 在插件端通过 Ghostscript 将 EPS 转为 PNG，绕过 Mogan 内部 EPS 转换路径空格问题
3. 添加 `eps-bbox-empty?` 函数检测空 bounding box，避免 gs 处理 0x0 页面时崩溃
4. 添加 `png-size` 函数检测生成的 PNG 尺寸，过滤 1x1 空图片
5. 更新 `init-tikz.scm` 引入 `(binary gs)` 依赖
6. 补充单元测试覆盖 PNG 尺寸解析和 EPS bounding box 检测

## 6 Why

Mogan STEM 使用 Goldfish Scheme 重写的 TikZ 插件最初发送 EPS 文件给 Mogan，由 Mogan 内部调用 Ghostscript 将 EPS 转为 PNG 显示。当用户主目录或临时路径包含空格时（如 macOS 上 `/tmp/Jack Yansong Li/.lolly/...`），Mogan 依赖的 tbox 进程启动器 `tb_process_init_cmd` 不支持 `\ `（反斜杠空格）转义，导致 Ghostscript 执行失败，图片无法显示。

此外，当 TikZ 代码生成空图形（如 `\draw (0,0);` 产生 0x0 bounding box）时，Ghostscript 拒绝处理 `0x0` 页面大小，抛出 `configurationerror`，并触发 Mogan 的 `Error: bad image size` 弹窗。

## 7 How

### 7.1 插件端 EPS 转 PNG

参考 Gnuplot 插件的模式，在 `tm-tikz.scm` 中增加 `run-gs` 函数，使用 Ghostscript 将 EPS 转为 PNG：

```
gs -dQUIET -dNOPAUSE -dBATCH -dSAFER -sDEVICE=pngalpha
   -dGraphicsAlphaBits=4 -dTextAlphaBits=4 -r144
   -sOutputFile=output.png -f input.eps
```

`init-tikz.scm` 通过 launcher 将 `gs` 二进制路径作为第六个参数传入 Goldfish 脚本。

### 7.2 空 bounding box 检测

`eps-bbox-empty?` 读取 EPS 文件的 `%%BoundingBox:` 行，解析四个坐标值。若 `x2 - x1 <= 0` 或 `y2 - y1 <= 0`，则判定为空图形，直接输出提示文本，不再调用 gs。

### 7.3 极小 PNG 过滤

`png-size` 读取 PNG 文件 IHDR chunk 的宽高字段（大端 32 位整数）。`flush-image` 在发送图片前检查：若 PNG 宽或高小于等于 1，则输出提示文本，避免 Mogan 处理 1x1 图片时弹窗。

### 7.4 单元测试

在 `tm-tikz-test.scm` 中补充：
- `png-dimensions-from-bv`：通过构造已知 bytevector 测试 PNG 头解析
- `eps-bbox-empty?`：通过写临时 EPS 文件测试空/非空 bounding box 检测
