# [2036] 修复 ParagraphFormat 重置「点一次没用，点第二次才行」并提取公共 reader

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- `ai-docs/qml/README.md` - QML 对话框开发规范（补充「本地真相表 reader」节）

## 2 任务相关的代码文件
- `TeXmacs/progs/utils/library/dialog-value-table.scm`（新增）- 本地真相表公共模块
- `TeXmacs/progs/generic/paragraph-format-widgets.scm` - 段落格式 scheme facade（核心修复）
- `TeXmacs/progs/fonts/font-new-widgets.scm` - 字体选择器 scheme facade（纯重构接入）
- `TeXmacs/tests/2036.scm`（新增）- 回归测试
- `src/Plugins/Qt/qml/ParagraphFormat.qml` - 段落格式 QML（注释更新）
- `ai-docs/qml/README.md` - 文档补充

## 3 如何测试

### 3.1 集成测试
```bash
xmake b stem
xmake r 2036                       # headless 冒烟（不跑断言链）
MOGAN_TEST_GUI=1 xmake r 2036      # 真实 GUI 跑断言链（12 条 check）
xmake b qml_load_test && xmake r qml_load_test   # QML 加载回归
```

### 3.2 手动验证
打开「格式→段落」「文档→段落」，改若干字段点重置，确认**一次**即生效（段落级回打开时值、文档级回全局默认）。

## 4 如何提交

```bash
xmake b stem
gf fmt --changed-since=main
xmake b qml_load_test && xmake r qml_load_test
MOGAN_TEST_GUI=1 xmake r 2036      # 期望 12 correct, 0 failed
```

## 5 What
1. 新增公共模块 `utils/library/dialog-value-table.scm`：把 FontSelector 的本地真相表 reader 骨架抽出来（`value-table-ref/set!/remove!/clean`，entry-key 由调用方自定义）。
2. FontSelector 纯重构接入（行为不变）。
3. ParagraphFormat 接入：register 填表、`meta` 读表、`set` 同步写表、reset/Cancel 把目标值写表、cleanup 清表——消除 reset 滞后。
4. 新增 `TeXmacs/tests/2036.scm` 钉死修复契约。
5. 更新 QML 注释与 README。

## 6 Why
ParagraphFormat 本应贯彻 FormDialog 的「真相源 = QML values、不重读树」，但 reset 那条路破例跨 eval 重读 `get-env`/`get-init`——读取滞后于树写入，于是 `resetValues()` 重建的 values 仍是撤销前旧值，表现为「点两次才生效」。文档里的生效值是对的，只有 QML 显示滞后。FontSelector 不重读树（有本地真相表），故把它提取成公共模块让 ParagraphFormat 接入。

## 7 How
本地真相表（`dialog-value-table`）：读取经 `value-table-ref` 表优先命中、缺项才 fallback 实时树；set/reset/Cancel 把目标值写入表，后续读即时生效、不碰树。

- **entry-key**：`(key var)`（ParagraphFormat）/ `(specs var buffer)`（FontSelector），模块对 key 形状不假设。
- **register**：打开时把各字段值写入表。
- **meta**：`value-table-ref (entry-key key var) (lambda () (getter var))`。
- **set**：`setter` 写树后 `value-table-set!` 同步写表。
- **reset**：段落级经 `restore-snapshot` 写全部快照值（**修滞后**——get-env 读排版产物 cur、滞后于树写入，是「点两次」根因，写表后 meta 读表绕开）；文档级 `init-default` 后用 `get-init` 算全局默认写表（get-init 直读 init/pre hashmap、**本就不滞后**，写表仅为两级统一读取路径 + 保持表与文档真相一致，非为修滞后）。
- **cleanup**：清该 key 全字段。

### review 后的收口（第二遍）
经整体 review + 对抗性 review agent，额外做了三处 + 推翻一处：

- **#4 revert 失效集对齐**：revert 文档分支先按 `paragraph-all-var-names`（含 par-left/right，与 init-default 写入集、cleanup 集同源）清缓存再重填 scope 字段——三集一致。
- **#6 restore-snapshot 写表保留 `(when old ...)` 守卫**：快照恒为非空 string，守卫实际恒真、是 no-op 防御；未移除，保持与上方树写回分支（同样有 `(when (and old ...))`）一致的写法。
- **#3 显式化契约**：模块注释写明「适用前提：开窗期间文档不被其它入口改动」（read-through 无外部失效钩子）+ falsy 前提（s7 把存 #f 当删除，故用 `(or ...)`）。
- **推翻 #1（falsy 修复）**：原想用 `hash-table-contains?` 区分缺项与缓存 #f，实测发现 s7 把「存 #f」当删除，根本无此态——`(or ...)` 本就正确，回退，改为在契约里记录此前提。测试相应从「测不可能态」改为测真实读写语义。

实施中还修了一处作用域 bug：文档级 revert 最初把 `for` 循环写在 `with (_ getter setter)` 块外导致 `getter` unbound，只炸「文档→段落」。

## 8 附带修复：FontSelector「Reset」恢复系统默认字体（非回到打开时）

**What**：QML 字体选择器的 Reset 按钮（`FontSelectorBridge::reset()` → `(font-selector-restore key)`）原实现是 `font-selector-revert-to-snapshot`——回到打开对话框瞬间的字体（`initial-snapshot`），机制同 Cancel。当文档本就设了非默认字体时，Reset 会回到那个「上一次选择的字体」而非系统默认，与用户对「Reset」的预期不符。

**Why**：`font-selector-restore` 与 Cancel 共用 revert-to-snapshot，两者语义被合并成「撤销本对话框改动」。但 Reset 用户预期是「恢复默认」（与老 tm-tool 字体对话框的「Restore defaults」按钮一致——`selector-restore specs #t` 走 `:default` 移除 init）。

**How**：把 `font-selector-restore` 改为走 `selector-restore specs #t`——`init-multi` 对各字体 var 走 `init-default` 移除 init（回到继承的全局/pref 默认），并清本地真相表，之后 `selector-get*` fallback 到 `initial-font-data`（`get-init` 直读 init 树、即时、无滞后）得系统默认值。Cancel 仍走 revert-to-snapshot（撤销本对话框改动），二者语义分离。

**验证**：`TeXmacs/tests/2028.scm` 加「reset restores system default font」步——预设非默认字体使快照 ≠ 系统默认，live 改 family 后 Reset，断言 family 回系统默认、`init-has? "font"` 为 `#f`（init 已移除）。`MOGAN_TEST_GUI=1 xmake r 2028` → 16 correct, 0 failed。

注：该 bug 在 main 即存在（本分支仅把 `selector-restore` 内的 `ahash-remove!` 换成等价的 `value-table-clean`，未改其语义）；此次顺带在 2036 分支修正 Reset 语义。

