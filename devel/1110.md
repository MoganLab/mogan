# [1110] 升级 pdfhummus 到 v4.9.0

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板

## 2 任务相关的代码文件
- `3rdparty/pdfhummus/**` —— pdfhummus 源码目录，整体替换为 PDF-Writer v4.9.0
- `3rdparty/pdfhummus/xmake.lua` —— pdfhummus 的 xmake 构建定义（处理 OpenSSL/图片后端等可选依赖）
- `3rdparty/pdfhummus.lua` —— `liii-pdfhummus` 包定义（后续从根 `xmake.lua` 迁出，见第 8 节）
- `3rdparty/libaesgm.lua` —— `liii-libaesgm` 包定义（后续从根 `xmake.lua` 迁出，见第 8 节）
- `xmake/vars.lua` —— 依赖版本汇总

## 3 如何测试

### 3.1 确定性测试（单元测试）
```bash
# 强制重新构建 pdfhummus 包
xmake require -y --force liii-pdfhummus
```

### 3.2 非确定性测试（文档验证）
```bash
# 重新配置并编译主项目
xmake f -y
xmake b stem

# 启动后验证 PDF 相关功能（如导出 PDF）仍可正常工作
xmake r stem
```

## 4 如何提交

提交前执行以下最少步骤：

```bash
gf fmt --changed-since=main
```

## 5 What
1. 将 `3rdparty/pdfhummus` 整体替换为 PDF-Writer v4.9.0 的源码（从本地克隆 `/home/da/git/PDF-Writer` 复制，节省下载时间）。
2. 将 `PDFHUMMUS_VERSION` 从 `4.6.2` 更新为 `4.9.0`。
3. 同步更新 `xmake/vars.lua` 中的版本常量。
4. 修复升级后 `liii-pdfhummus` 包在未启用 OpenSSL 时编译失败的问题：v4.9.0 新增的
   `InputAESDecodeStreamSSL` / `OutputAESEncodeStreamSSL` 头文件无条件 `#include <openssl/evp.h>`，
   而项目 `add_files("PDFWriter/*.cpp")` 会把它们一并编译，导致找不到该头文件。

## 6 Why
PDF-Writer v4.9.0 包含了上游的若干 bug 修复与改进（如加密、流处理等），
升级到该版本可以让 Mogan 的 PDF 导出/解析功能跟上上游进度。
本地已存在 `/home/da/git/PDF-Writer` 克隆，复用该目录可以显著节省下载与解压时间。

## 7 How
1. 在本地克隆中检出 `v4.9.0` 标签：
   ```bash
   git -C /home/da/git/PDF-Writer checkout v4.9.0
   ```
2. 将本地克隆中的 `PDFWriter` 目录与顶层 `xmake.lua`、`CMakeLists.txt`、`Config.cmake.in`、
   `.gitattributes`、`.gitignore`、`LICENSE`、`LicenseTemplate.txt`、`readme.md`、`cmake/`
   复制到 `3rdparty/pdfhummus/`，覆盖旧版本（不复制捆绑的 `FreeType`、`LibAesgm` 等子模块，
   因为 xmake 使用外部包）。
3. 修改 `xmake.lua` 中的 `PDFHUMMUS_VERSION`。
4. 同步修改 `xmake/vars.lua` 中的 `PDFHUMMUS_VERSION`。
5. 在 `3rdparty/pdfhummus/xmake.lua` 中，未启用 OpenSSL 时用 `remove_files` 跳过
   `InputAESDecodeStreamSSL.cpp` / `OutputAESEncodeStreamSSL.cpp`（与上游
   `PDFWriter/CMakeLists.txt` 的二选一逻辑一致，改用纯软件实现的 `Input/OutputAESEncodeStream`）。
6. 运行 `xmake require -y --force liii-pdfhummus` 或 `xmake f -y` 验证包能正常构建。

## 8 后续：包定义迁移到 3rdparty 目录（2026/06/26）

照 `3rdparty/doctest.lua` 的模式，把原本内联在根 `xmake.lua` 里的两个 `package` 定义抽到独立文件：

- `liii-libaesgm` → `3rdparty/libaesgm.lua`
- `liii-pdfhummus` → `3rdparty/pdfhummus.lua`

根 `xmake.lua` 通过 `includes("3rdparty/libaesgm.lua")` 与 `includes("3rdparty/pdfhummus.lua")` 引入，紧挨已有的 `includes("3rdparty/doctest.lua")`。

**动机**：`liii-doctest` 早已拆到 `3rdparty/doctest.lua`，但 `liii-libaesgm` / `liii-pdfhummus` 仍内联在根 `xmake.lua` 里，破坏了「每个 vendored 第三方包一个独立 `.lua` 文件」的约定。补齐后根 `xmake.lua` 只保留全局配置与项目入口。

**注意点**：`set_sourcedir` 的路径相对调整——原先 `os.scriptdir()` 指向仓库根，写的是 `"3rdparty/libaesgm"`；迁到 `3rdparty/xxx.lua` 后 `os.scriptdir()` 指向 `3rdparty/`，故改为 `"libaesgm"` / `"pdfhummus"`。顺手删除 `xmake.lua` 里冗余的 `PDFHUMMUS_VERSION = "4.9.0"`（已在 `xmake/vars.lua:34` 定义）。

验证：`xmake b stem`。

