# 1193 规范化插件模块命名

## 任务 8：插件 binary 定义模块命名空间规范化

### What

4 个插件的 binary 定义模块从共享命名空间 `(binary ...)` 改为插件专属 `(<plugin> <plugin>-binary)`：

| 插件 | 旧模块名 | 新模块名 | 文件位置变更 |
|------|---------|---------|------------|
| gnuplot | `(binary gnuplot)` | `(gnuplot gnuplot-binary)` | `progs/binary/` → `progs/gnuplot/gnuplot-binary.scm` |
| maxima | `(binary maxima)` | `(maxima maxima-binary)` | `progs/binary/` → `progs/maxima/maxima-binary.scm` |
| julia | `(binary julia)` | `(julia julia-binary)` | 从共享 binary 插件移至 `progs/julia/julia-binary.scm` |
| python | `(binary python3)` | `(python python-binary)` | 从共享 binary 插件移至 `progs/python/python-binary.scm` |

同时将 tikz 插件内的 pdflatex 定义 `(binary pdflatex)` 移至共享 binary 插件
（pdflatex 是通用工具，非 tikz 专属）。

### Why

插件内模块命名规范：必须以插件名开头。`(binary gnuplot)` 等占据 binary 命名空间，
违反规范。改为 `(<plugin> <plugin>-binary)` 后与 language 模块 `(<plugin> <plugin>-lang)`
和 format 模块 `(<plugin> <plugin>-format)` 命名模式一致。

### How

- 模块声明从 `(texmacs-module (binary <name>) ...)` 改为 `(<plugin> <plugin>-binary)`
- 文件从 `progs/binary/<name>.scm` 迁移到 `progs/<plugin>/<plugin>-binary.scm`
- 各 init 文件中 `use-modules` 引用同步更新

### 涉及文件

- `TeXmacs/plugins/gnuplot/progs/gnuplot/gnuplot-binary.scm`（改名+重命名）
- `TeXmacs/plugins/gnuplot/progs/init-gnuplot.scm`
- `TeXmacs/plugins/maxima/progs/maxima/maxima-binary.scm`（改名+重命名）
- `TeXmacs/plugins/maxima/progs/init-maxima.scm`
- `TeXmacs/plugins/julia/progs/julia/julia-binary.scm`（从 binary 插件移入）
- `TeXmacs/plugins/julia/progs/init-julia.scm`
- `TeXmacs/plugins/python/progs/python/python-binary.scm`（从 binary 插件移入）
- `TeXmacs/plugins/python/progs/init-python.scm`
- `TeXmacs/plugins/binary/progs/binary/pdflatex.scm`（从 tikz 插件移入）

### 如何测试

`xmake b stem` 构建通过即可（binary 查找逻辑不变，仅模块名变更）。

## 任务 5：cite 插件

### What

cite 插件的模块从 `(contrib cite cite-sort)` / `(contrib cite cite-sort-test)` 改为
`(cite cite-sort)` / `(cite cite-sort-test)`。

### Why

插件内模块命名规范：必须以插件名开头。`(contrib cite ...)` 占据 contrib 命名空间，
违反规范。

### How

- 模块文件迁移：`progs/contrib/cite/cite-sort.scm` + `cite-sort-test.scm`
  → `progs/cite/cite-sort.scm` + `cite-sort-test.scm`，
  `texmacs-module` 声明及模块间 `:use` 同步改一级名
- 更新全部引用：
  - `packages/contrib/cite-sort.ts` 的 `<use-module>`
  - `doc/cite.en.tmu` 的 `(use-modules ...)`

### 如何测试

`xmake b stem && xmake r cite-sort-test`（纯逻辑测试，headless 秒级反馈）。

## 任务 4：语言插件模块命名空间规范化

### What

19 个语言插件的模块一级命名空间从共享的 `code` 改为插件专属命名：

| 插件 | 旧模块名 | 新模块名 |
|------|---------|---------|
| bash | `(code bash-edit\|bash-lang\|bash-mode)` | `(bash bash-edit\|bash-lang\|bash-mode)` |
| json | `(code json-lang)` | `(json json-lang)` |
| csv | `(code csv-lang)` | `(csv csv-lang)` |
| java | `(code java-edit\|java-lang)` | `(java java-edit\|java-lang)` |
| javascript | `(code javascript-lang)` | `(javascript javascript-lang)` |
| lua | `(code lua-edit\|lua-lang\|lua-mode)` | `(lua lua-edit\|lua-lang\|lua-mode)` |
| r | `(code r-edit\|r-lang\|r-mode)` | `(r r-edit\|r-lang\|r-mode)` |
| r7rs | `(code r7rs-edit\|r7rs-lang\|r7rs-keyword\|srfi-keyword)` | `(r7rs r7rs-edit\|r7rs-lang\|r7rs-keyword\|srfi-keyword)` |
| scala | `(code scala-edit\|scala-lang\|scala-mode)` | `(scala scala-edit\|scala-lang\|scala-mode)` |
| sql | `(code sql-edit\|sql-lang\|sql-mode)` | `(sql sql-edit\|sql-lang\|sql-mode)` |
| gnuplot | `(code gnuplot-lang)` | `(gnuplot gnuplot-lang)` |
| fish | `(code fish-edit\|fish-lang\|fish-mode)` | `(fish fish-edit\|fish-lang\|fish-mode)` |
| goldfish | `(code goldfish-edit\|goldfish-lang\|goldfish-ast-lang\|liii-keyword)` | `(goldfish goldfish-edit\|goldfish-lang\|goldfish-ast-lang\|liii-keyword)` |
| julia | `(code julia-lang)` | `(julia julia-lang)` |
| moonbit | `(code moonbit-edit\|moonbit-lang\|moonbit-mode)` | `(moonbit moonbit-edit\|moonbit-lang\|moonbit-mode)` |
| tikz | `(code tikz-edit\|tikz-lang\|tikz-mode)` | `(tikz tikz-edit\|tikz-lang\|tikz-mode)` |
| matlab | `(code matlab-edit\|matlab-lang\|matlab-mode)` | `(matlab matlab-edit\|matlab-lang\|matlab-mode)` |
| python | `(code python-edit\|python-lang)` | `(python python-edit\|python-lang)` |
| cpp | `(code cpp-edit\|cpp-lang)` | `(cpp cpp-edit\|cpp-lang)` |

### Why

插件模块的一级命名空间从共享的 `code` 改为插件专属名（`bash`、`python` 等），
缩小 `use-modules` 查找模块时的搜索范围。例如 `(bash bash-edit)` 只需在 load
path 上查找 `bash/bash-edit.scm`，只有 bash 插件有该目录，而非 `(code bash-edit)`
需要在每个插件的 `progs/code/` 子目录下逐一查找。随着插件数量增长，收益递增。

### How

- 每个插件的模块文件从 `progs/code/` 迁移到 `progs/<name>/`
- `texmacs-module` 声明及模块间 `:use` 同步改一级名
- 对应的 `packages/code/<name>.ts`（或 `.stem`）的 `<use-module>` 更新
- goldfish 额外修复对 r7rs 已改名模块的跨插件引用
- C++ 侧直接使用 `<name> <name>-lang` 命名空间：
  - `prog_language_rep` 构造器直接 `(use-modules (<name> <name>-lang))`
  - `prog_lang_exists` 移除 `progs/code/` 旧路径，仅保留 `progs/<name>/<name>-lang.scm` 新路径
  - `prog-kbd.scm` 中 cpp 引用同步更新

### 涉及文件

共 47 个模块文件 rename + edit，18 个 `.ts`/`.stem` 编辑，外加 `init-tikz.scm`、
`python.stem` 的引用更新，以及 `src/System/Language/prog_language.cpp` 的 C++ 改动。

### 如何测试

点击 **帮助 → 插件**，在插件列表中逐一打开各语言插件的文档页面，确认文档能正常加载
且语法高亮、缩进等功能无异常。涉及的插件：bash、json、csv、java、javascript、lua、
r、r7rs、scala、sql、gnuplot、fish、goldfish、julia、moonbit、tikz、matlab、python、
cpp。

## 任务 3：comment 插件

### What

comment 插件的四个模块一级模块名从 `various` 改为 `comment`：
`(various comment-drd|comment-edit|comment-menu|comment-widgets)`
→ `(comment comment-drd|comment-edit|comment-menu|comment-widgets)`。

### Why

插件内模块命名规范：必须以插件名开头。`(various comment-*)` 占据 various
命名空间，违反规范。

### How

- 模块文件迁移：`TeXmacs/plugins/comment/progs/various/`
  → `TeXmacs/plugins/comment/progs/comment/`，
  `texmacs-module` 声明及模块间 `:use` 同步改一级名
- 更新全部引用：
  - `TeXmacs/plugins/comment/packages/utilities/comment.ts` 的 `<use-module>`
  - `TeXmacs/progs/text/text-menu.scm` 的 `:use`
  - `TeXmacs/plugins/keyboard/progs/keyboard/text-kbd-utf8.scm` 的 `:use`
  - `TeXmacs/progs/kernel/texmacs/tm-secure.scm` 的 `lazy-define`

## 任务 2：autosave 插件

### What

autosave 插件的模块从 `(plugin autosave)` / `(plugin autosave-impl)` 合并改名为
`(autosave plugin)`。

### Why

插件内模块命名规范：必须以插件名开头。`(plugin autosave)` 占据 plugin 命名空间，
违反规范。原 entry/impl 双模块拆分是为规避 use-modules 直载模块含 `(import ...)`
的问题，account 插件（任务 1）已验证单模块内直接用 import 可行，故合并为单模块。

### How

- 模块文件迁移合并：`progs/plugin/autosave.scm` + `progs/plugin/autosave-impl.scm`
  → `TeXmacs/plugins/autosave/progs/autosave/plugin.scm`，
  `texmacs-module` 声明改为 `(autosave plugin)`，删除 `progs/plugin/` 目录
- 更新全部引用：
  - `TeXmacs/plugins/autosave/progs/init-autosave.scm`
  - `TeXmacs/progs/texmacs/menus/preferences-tools.scm`
  - `TeXmacs/progs/texmacs/menus/preferences-menu.scm`
  - `TeXmacs/progs/texmacs/menus/preferences-widgets.scm`
  - `TeXmacs/progs/texmacs/texmacs/tm-server.scm`
  - `TeXmacs/progs/texmacs/texmacs/tm-files-test.scm`

## 任务 1：account 插件

### What

account 插件的模块从 `(liii account)` 改名为 `(account liii)`。

### Why

插件内模块命名规范：必须以插件名开头。`(liii account)` 占据 liii 命名空间，
违反规范。

### How

- 模块文件迁移：`TeXmacs/plugins/account/progs/liii/account.scm`
  → `TeXmacs/plugins/account/progs/account/liii.scm`
- `texmacs-module` 声明改为 `(account liii)`
- 更新全部引用：
  - `TeXmacs/progs/generic/generic-edit.scm` 的 `(use-modules (liii account))`
  - `src/Plugins/Qt/QTMOAuth.cpp`、`src/Plugins/Qt/qt_tm_widget.cpp`
    中 `eval ("(use-modules (liii account))")` 各调用点

## 任务 7：data format 模块

### What

27 个 data format 模块的一级命名空间从共享的 `data` 改为插件专属命名：

| 插件 | 旧模块名 | 新模块名 |
|------|---------|---------|
| bash | `(data bash)` | `(bash bash-format)` |
| csv | `(data csv)` | `(csv csv-format)` |
| data | `(data code)` / `(data image)` | `(data code-format)` / `(data image-format)` |
| docx | `(data docx)` | `(docx docx-format)` |
| fish | `(data fish)` | `(fish fish-format)` |
| gnuplot | `(data gnuplot)` | `(gnuplot gnuplot-format)` |
| goldfish | `(data goldfish)` | `(goldfish goldfish-format)` |
| html | `(data html)` | `(html html-format)` |
| java | `(data java)` | `(java java-format)` |
| javascript | `(data javascript)` | `(javascript javascript-format)` |
| json | `(data json)` | `(json json-format)` |
| julia | `(data julia)` | `(julia julia-format)` |
| latex | `(data latex)` / `(data bibtex)` | `(latex latex-format)` / `(latex bibtex-format)` |
| lua | `(data lua)` | `(lua lua-format)` |
| matlab | `(data matlab)` | `(matlab matlab-format)` |
| moonbit | `(data moonbit)` | `(moonbit moonbit-format)` |
| python | `(data python)` | `(python python-format)` |
| r | `(data r)` | `(r r-format)` |
| r7rs | `(data r7rs)` | `(r7rs r7rs-format)` |
| scala | `(data scala)` | `(scala scala-format)` |
| sql | `(data sql)` | `(sql sql-format)` |
| stem | `(data stem)` | `(stem stem-format)` |
| stm | `(data stm)` | `(stm stm-format)` |
| tikz | `(data tikz)` | `(tikz tikz-format)` |
| tmu | `(data tmu)` | `(tmu tmu-format)` |

### Why

data format 模块（`(data csv)`、`(data fish)` 等）共享 `data` 命名空间，违反
"插件模块一级命名必须以插件名开头"的规范。改为 `(<plugin> <name>-format)` 后与
任务 4（language 模块）命名模式一致。

### How

- 每个插件的 format 文件从 `progs/data/<name>.scm` 迁移到 `progs/<name>/<name>-format.scm`
- `texmacs-module` 声明从 `(data <name>)` 改为 `(<plugin> <name>-format)`
- `init-research.scm` 中 9 处 `lazy-format` 同步更新
- 7 个插件 init 文件中 `lazy-format` 引用更新
- 16 个 `.ts`/`.stem` 文件中 `<use-module>` 更新
- C++ 侧 `tracked_totex.cpp` 中 `(use-modules (data latex))` 更新
- 4 个测试文件中 `use-modules` 引用更新
- `database/bib-db.scm` 和 `llm/` 下 2 个文件的 `:use` 引用更新
- 删除旧 `progs/data/` 目录下的 27 个旧文件

### 涉及文件

共 60 个文件变更。

### 如何测试

`xmake b stem && xmake r 70_7`（19 项测试通过，覆盖 format-from-suffix/format?/format-get-name）。

## 任务 6：code 插件

### What

code 插件 `progs/prog/` 下的 3 个模块从共享命名空间 `prog` 改为插件专属 `code`：

| 旧模块名 | 新模块名 |
|---------|---------|
| `(prog prog-drd)` | `(code prog-drd)` |
| `(prog prog-kbd)` | `(code prog-kbd)` |
| `(prog prog-mode)` | `(code prog-mode)` |

### Why

插件内模块命名规范：必须以插件名开头。`(prog prog-*)` 占据 prog 命名空间，
且与 `TeXmacs/progs/prog/` 核心模块容易混淆。

### How

- 模块文件迁移：`progs/prog/prog-*.scm` → `progs/code/prog-*.scm`
- `texmacs-module` 声明改为 `(code prog-*)`
- `prog-kbd.scm` 内部 `:use (prog prog-mode)` 同步更新
- 外部引用更新：
  - `init-code.scm` 的 `lazy-keyboard` 引用
  - `TeXmacs/progs/text/text-drd.scm` 的 `:use` 引用

### 涉及文件

- `TeXmacs/plugins/code/progs/code/prog-drd.scm`（新）
- `TeXmacs/plugins/code/progs/code/prog-kbd.scm`（新）
- `TeXmacs/plugins/code/progs/code/prog-mode.scm`（新）
- `TeXmacs/plugins/code/progs/prog/`（删）
- `TeXmacs/plugins/code/progs/init-code.scm`
- `TeXmacs/progs/text/text-drd.scm`

### 如何测试

`xmake b stem && xmake r scheme-tools-test`（7 项测试通过）。
