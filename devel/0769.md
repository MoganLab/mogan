# [0769] 修复 Collab 模块导致 ImGui 构建菜单崩溃
## What
在 glue_plugins_extra 中定义了 loro_enabled，用于在 scheme 中得到 loro 是否启用，避免错误地构建 loro 相关菜单

## 如何测试
```
xmake f --loro=no --qt_frontend=no
xmake b stem
xmake r stem
```
使用鼠标打开 `File` 菜单，菜单正常打开，并且没有 Collaborative 子菜单
