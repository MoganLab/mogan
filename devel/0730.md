# [0730] WASM (ImGui) 键盘事件适配
## What
添加了 ImGui 层的转发逻辑，适配键盘事件
添加了 JS 脚本用于检测浏览器 OS 标识，适配操作系统

## 如何测试
```
xmake f -p wasm --qt_frontend=no --pdfhummus=no
xmake b stem
xmake r stem
```
打开浏览器，在自动打开的文档中进行操作，验证快捷键是否工作（例如 cmd-z cmd-shift-z）
