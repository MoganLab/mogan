# [1311] 在领取会员左侧添加 Go 按钮并用 QML 实现 Go 菜单

## 1 任务背景
在 Mogan 标题栏（领取会员按钮左侧）新增一个导航/跳转入口按钮（Go），提供快速打开最近使用文档的功能。
- 移除光标历史跳转（后退、前进、保存位置）及冗余的打开文档列表；
- 专一、聚焦展示最近使用的文档列表（Recent），点击快速加载；
- 采用现代 QML 实现该下拉菜单浮层，并从主菜单栏中移除原本的「Go」菜单项。

## 2 涉及的代码文件
- `devel/1311.md` — 本任务说明文档
- `src/Plugins/QWindowKit/windowbar_p.hpp` — WindowBarItem 枚举添加 GoButton（位于 TitleLabel 与 VipButton 之间）
- `src/Plugins/QWindowKit/windowbar.hpp` — 声明 setGoButton / goButton / takeGoButton
- `src/Plugins/QWindowKit/windowbar.cpp` — 实现 setGoButton / goButton / takeGoButton
- `src/Plugins/Qt/qt_tm_widget.hpp` — 添加 goButton 成员与 showGoMenu 方法声明
- `src/Plugins/Qt/qt_tm_widget.cpp` — 初始化 goButton 按钮、连接点击事件并弹出 QML 菜单浮层（右对齐）
- `src/Plugins/Qt/QTMTabPage.cpp` — 调整 arrangeTabPages 中的 reservedRight，为 Go 按钮预留宽度
- `src/Plugins/Qt/GoMenuBridge.hpp` — Go 菜单 C++↔QML bridge 声明
- `src/Plugins/Qt/GoMenuBridge.cpp` — Go 菜单 bridge 实现（从 Scheme 拉取元数据并支持 loadBuffer）
- `src/Plugins/Qt/qml/GoMenu.qml` — Go 菜单 QML 浮层实现（纯净最近文档列表）
- `src/Plugins/Qt/moganqml.qrc` + `src/Plugins/Qt/qml/qmldir` — 注册 GoMenu.qml
- `TeXmacs/progs/texmacs/menus/file-menu.scm` — Scheme 侧实现 (go-menu-meta)、(go-menu-switch-to-buffer)、(go-menu-load-buffer)
- `TeXmacs/progs/texmacs/menus/main-menu.scm` — 移除主菜单中的 Go 菜单项
- `TeXmacs/progs/prog/scheme-menu.scm` — 移除备用/Scheme 菜单中的 Go 菜单项
- `TeXmacs/misc/themes/liii.css` & `liii-night.css` — 增加 #go-button 样式
- `TeXmacs/plugins/lang/dic/en_US/zh_CN.scm` — 增加 "open documents" 词条翻译
- `tests/Plugins/Qt/qml_load_test.cpp` — 增加 GoMenu.qml 的加载单元测试
- `TeXmacs/tests/python/1311.py` — 自动化 UI 测试脚本（自动定位点击「转到」按钮并截图验证浮层展示）
- `src/Texmacs/Data/new_view.cpp` — 修复 `delete_view` 未重置 `the_view` 导致关闭草稿文档时野指针崩溃

## 3 下拉菜单展示改进
通过 Python 自动化测试脚本截取真实界面截图，进行以下精简与视觉体验提升：
1. **纯净聚焦最近使用**：移除前进、后退、保存位置三项操作，仅展示「最近使用」文档，去除冗余图标占位，文件名靠左完整对齐；
2. **初始高度自适应**：QML `Column.implicitHeight` 依赖延迟排版导致 `adjustSize()` 初始高度塌陷，通过依据条目数即时计算确定合理高度；
3. **中文文件名编码修复**：`GoMenuBridge.cpp` 中将 `cork_to_utf8` 替换为 `to_qstring`，自动保持 UTF-8 文件名保真，杜绝乱码；
4. **浮层右对齐**：右对齐至「转到」按钮右侧，自然向左展开，完全不遮挡「领取会员」等右侧按钮；
5. **视觉与滚动**：优化双层柔和阴影，超出最大高度时显示优雅细条滚动指示器；
6. **点击打开文档崩溃修复**：点击打开最近文档时，`load-buffer-open` 会自动关闭之前的未修改空白草稿缓冲区（`prev-buffer`），而 `delete_view` 在销毁旧视图时未重置全局指针 `the_view`，导致随后的光标移动通知触发 `get_current_view()` 时访问已释放内存崩溃（SIGSEGV）；修复为在 `delete_view` 中重置 `the_view = NULL`，并在 `get_current_view` 中安全回退到历史可用视图，同时在 `abstract_view` 中增加空指针保护；另外将 Scheme 侧 `load-buffer` 替换为更健壮的 `load-document`。

## 4 如何测试

### 4.1 QML 加载单元测试
```bash
xmake b qml_load_test && xmake r qml_load_test
```

### 4.2 Python 端到端自动化 UI 测试
```bash
python3 TeXmacs/tests/python/1311.py
```
自动定位并点击标题栏「转到」按钮，触发 QML 下拉浮层，完成内容检测与截图（保存至 `/tmp/1311_go_menu_popup.png`），并验证按 Esc 优雅关闭。

### 4.3 手动测试
启动 Mogan STEM：
```bash
xmake b stem && xmake r stem
```
1. 观察主菜单栏（File, Edit, View, Document...），确认「Go」菜单项已被移除；
2. 观察窗口标题栏右上角（领取会员按钮左侧），可见「转到」/「Go」按钮；
3. 点击该按钮，下方弹出优雅的现代 QML 菜单浮层；
4. 菜单内展示：
   - 后退（若无历史置灰）、前进（若无未来置灰）、保存位置；
   - 打开的文档列表，当前文档前带勾号，点击可切换标签页；
   - 最近使用的文档列表，点击可打开文档；
5. 点击菜单外部或按 Esc 键，菜单自动关闭。
