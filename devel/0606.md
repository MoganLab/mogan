# [0606] 添加 \to 命令支持

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [0605.md](0605.md) - 参考任务（stack 结构 LaTeX 导出修复）

## 2 任务相关的代码文件
- `TeXmacs/packages/standard/std-math.ts`
- `TeXmacs/tests/0606.scm`

## 3 如何测试

### 3.1 确定性测试（单元测试）
```bash
xmake run 0606
```

### 3.2 非确定性测试（文档验证）
1. 打开 Mogan STEM，进入数学模式
2. 输入 `<to>` 后回车，确认显示为右箭头符号（与 `<rightarrow>` 一致）
3. 在函数映射场景中使用（如 `f: X <to> Y`），确认排版正确

## 4 如何提交

提交前执行以下最少步骤：

```bash
xmake run 0606
```

## 5 What
在 Mogan STEM 的数学模式中添加 `\to` 命令支持，使其作为 `\rightarrow` 的简写别名，与 LaTeX 的 `\to` 命令行为一致。

1. 在 `std-math.ts` 中新增 `to` 宏定义，将其映射为 `<rightarrow>`
2. 添加集成测试 `0606.scm`，验证 `to` 宏的正确性

## 6 Why

LaTeX 中 `\to` 是 `\rightarrow` 的标准简写形式，广泛用于函数映射表示（如 `f: X \to Y`）。Mogan STEM 此前仅支持 `<rightarrow>` 而不支持 `<to>`，导致：
- 从 LaTeX 迁移的用户需要改变输入习惯
- 函数映射表达式的输入不够便捷
- 与 LaTeX 的兼容性存在缺口

## 7 How

实现思路：

1. 在 `std-math.ts` 的数学宏包定义区域添加 `<assign|to|<macro|\<rightarrow\>>>`，将 `to` 定义为 `rightarrow` 的宏别名
2. 该宏定义遵循 Mogan 宏系统的标准语法，与 `std-math.ts` 中其他数学符号宏定义保持一致
3. 由于 `rightarrow` 已在字体渲染和符号系统中完整定义，`to` 宏可直接复用其渲染能力，无需额外字体或键盘映射配置
