# [1202] environment/env.ts 及其加载的包转换为 .stem

## 相关文档
- `devel/1131.md` —— stem 格式定义与此前 `.ts` → `.stem` 全量转换流程（蓝本）

## 任务相关的代码文件
- `TeXmacs/packages/environment/env.ts` 及同目录全部包：
  - `env-base.ts`、`env-math.ts`、`env-theorem.ts`（→ `env-enunciation.ts`）、
    `env-float.ts`、`env-program.ts`（→ `env-pseudo-code.ts`）、
    `env-flush-math.ts`（独立包，加载 `env-math`）
- `TeXmacs/progs/generic/tests/style-package-load-test.scm` —— headless 测试

## 如何测试
1. 转换后运行 `xmake r style-package-load-test` 与 `xmake r 70_7`。
2. 启动 Mogan，新建文档，确认 theorem、equation 等环境可用（`env` 包由
   `generic.ts` 等样式间接加载）。

## 2026/08/13 实现

### What
将 `TeXmacs/packages/environment/` 目录下全部 9 个 `.ts` 转换为 `.stem`
（Scheme S-expression 格式），删除原 `.ts`。

### Why
- 延续 1131 的 `.ts` → `.stem` 迁移：`.stem` 便于版本控制 diff/merge，
  可被外部工具直接解析。
- 此前 plugins 与 `packages/customize/language/` 已完成迁移，
  `packages/environment/` 是核心样式包中仍使用 `.ts` 的部分。

### How
- 转换集合：`env.ts` + 其 `use-package` 传递加载的 7 个包
  （`env-base`、`env-math`、`env-theorem` → `env-enunciation`、
  `env-float`、`env-program` → `env-pseudo-code`），加上同目录独立的
  `env-flush-math.ts`，共 9 个文件，整个目录不再有 `.ts`。
- 复用 1131 的转换流程，headless 脚本如下（注意 `string-load` 需要绝对路径，
  相对路径会静默失败返回空串）：

  ```scheme
  (define dir "/home/da/git/mogan/TeXmacs/packages/environment/")
  (define names
    '("env" "env-base" "env-math" "env-theorem"
      "env-enunciation" "env-float" "env-program" "env-pseudo-code"
      "env-flush-math"))

  (for-each
   (lambda (name)
     (let* ((ts (string-append dir name ".ts"))
            (stem (string-append dir name ".stem"))
            (t (parse-texmacs (string-load ts)))
            (out (convert t "texmacs-tree" "stem-document")))
       (string-save out stem)
       (display* "converted " ts " -> " stem "\n")))
   names)

  (quit-TeXmacs)
  ```

  运行方式：

  ```bash
  TEXMACS_PATH=$(pwd)/TeXmacs \
    build/linux/x86_64/release/moganstem \
    -headless -x '(load "/tmp/convert_env.scm")'
  ```

- roundtrip 验证：对每个文件比较
  `(parse-texmacs (string-load ts))` 与
  `(convert (string-load stem) "stem-document" "texmacs-tree")`，
  9 个文件全部 `==` 通过后，逐个 `gf fmt` 格式化并删除原 `.ts`。
- 无需改动 C++/Scheme 加载逻辑：1131 已使 `exec_use_package` /
  `resolve_style_file` / `url-resolve-package` 等优先 `.stem`、回退 `.ts`。
- 扩展 `style-package-load-test.scm`：断言 `env.stem`/`env-base.stem` 存在、
  对应 `.ts` 已删除（6 checks 全过）。

### 涉及文件
- `TeXmacs/packages/environment/*.ts`（9 个删除）
- `TeXmacs/packages/environment/*.stem`（9 个新增）
- `TeXmacs/progs/generic/tests/style-package-load-test.scm`
- `devel/1202.md`

## 2026/08/13 性能验证

### 方法
`use-package` 的 bench 只在 GUI typeset 路径触发，headless 无法复现，
改为对解析入口做微基准：`env` 闭包 8 个文件 × 20 轮，比较
`parse-texmacs`（.ts）与 `convert ... "stem-document"`（.stem）：

- 第 1 轮：ts 189 ms / stem 175 ms
- 第 2 轮：ts 234 ms / stem 200 ms

### 结论
`.stem` 解析比 `.ts` 快约 7%~15%（尽管文件体积 134 KB vs 59 KB，约 2.3 倍），
S-expression 解析器比 TeXmacs tag 解析器更高效。但 `use-package` 总耗时中
定义求值占大头且与格式无关，解析收益被稀释——实际测试端到端差别不大。
转换的意义在于 diff/merge 友好与外部工具可解析，而非性能。
用户 GUI 实测：`use-package environment/env` 62 ms（转换后）。
