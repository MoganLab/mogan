# [0602] 自定义快捷键提示样式 & 修复 macOS Qt6 快捷键符号颠倒问题

## 1 相关文档
- [0602.md](0602.md) - 任务文档
- [dddd.md](dddd.md) - 任务文档模板

## 2 任务相关的代码文件
- `TeXmacs/progs/utils/edit/auto-close.scm`
- `TeXmacs/progs/texmacs/menus/preferences-widgets.scm`
- `src/Plugins/Qt/qt_ui_element.cpp`
- `src/Plugins/Qt/qt_utilities.cpp`
- `src/Texmacs/Server/tm_config.cpp` (前置改动已回滚)
- `TeXmacs/plugins/lang/dic/en_US/zh_CN.scm`

## 3 如何测试

### 3.1 确定性测试（单元测试）
目前该功能主要通过 UI 和后端渲染，无需单元测试。

### 3.2 非确定性测试（文档验证）
1. 编译并运行 Mogan STEM：
   ```bash
   xmake b stem && xmake r stem
   ```
2. 打开「用户首选项」（User Preferences）->「键盘」（Keyboard）标签页。
3. 验证是否有一个名为「快捷键提示类型」（Keyboard shortcut style）的下拉选项。
4. 验证中英文显示。
5. 选择不同的选项（默认、文本、符号），点击后应该会提示重新启动应用以使更改生效。
6. 重新启动后，在菜单项和编辑区提示中验证快捷键显示样式：
   - **默认 (Default)**：macOS 默认显示原生符号（`⌘`、`⌃`、`⌥` 等），Windows/Linux 默认显示文字。
   - **文本 (Text)**：所有系统显示文字（Mac 下的 `make-align` 即 `"C-$"` 正确显示为 `Ctrl+$`，Save 显示为 `Cmd+S`）。
   - **符号 (Symbol)**：所有系统显示符号。即使在 Windows/Linux，菜单中也会强制使用 Unicode 符号 `⌃⌥⇧⌘`。

## 4 如何提交

提交前执行以下最少步骤：

```bash
# 格式化 Scheme 代码和变更
gf fmt --changed-since=main
# 确保构建通过
xmake b stem
```

## 5 What
1. 在用户首选项的「键盘」选项卡中，增加了一个「快捷键提示类型」（Keyboard shortcut style）选项，使用户可以在「默认」、「文本」、「符号」三种快捷键提示形式中自由选择，并且提供了中文翻译。
2. 修复了 macOS 下由于 Qt 6 的修饰键兼容映射导致的「文字模式下 Ctrl 与 Cmd 文字颠倒」以及「默认/符号模式下 Ctrl 与 Cmd 符号错乱」的底层 Bug，完美统一了菜单快捷键符号、文字与用户实际物理按键的对应关系。

## 6 Why
1. 目前 Mogan 快捷键的显示是根据系统强制绑定的，在 macOS 强制使用符号显示，而在 Windows/Linux 强制使用文字显示。提供该配置可使用户根据个人偏好自主定制显示样式。
2. 在升级到 Qt 6 后，Qt 在 macOS 上将 `Qt::ControlModifier` 映射到了物理 Control 键，`Qt::MetaModifier` 映射到了物理 Command 键。而 Mogan 原来的 C++ 键盘映射层仍保留着 Qt4/5 时代对两个修饰键进行对冲交换的逻辑。若不将文本渲染层的映射重新对冲，将导致文字样式在 macOS 平台下的 Ctrl 和 Cmd 完全显示反。

## 7 How
1. **Scheme 偏好层**：
   - 在 `auto-close.scm` 中声明 `"keyboard shortcut style"` 偏好。
   - 在 `preferences-widgets.scm` 中注册 pretty-names 并添加到「键盘首选项」部件，使用 `set-pretty-preference*` 函数以支持重启提示。
   - 在 `zh_CN.scm` 中添加中文翻译 `"快捷键提示类型"`。
2. **C++ 文本渲染兼容层 (`qt_ui_element.cpp`)**：
   - 保持 macOS 下 C++ 原生对冲键位交换以兼容 Qt 内置解析（确保默认 Symbol 模式下 NativeText 的 `⌘` 与 `⌃` 精确显示）。
   - 针对 macOS 纯文本选项，将 `QKeySequence::PortableText` 输出的修饰键名利用中间占位变量，进行双重反转：
     - `"Meta+"` $\rightarrow$ `"Ctrl_TEMP+"`
     - `"Ctrl+"` $\rightarrow$ `"Cmd+"`
     - `"Ctrl_TEMP+"` $\rightarrow$ `"Ctrl+"`
     - 确保了 Control 键物理上被识别为 `"C-"` 时显示为 `"Ctrl+"`，而 Command 键物理上被识别为 `"M-"` 时显示为 `"Cmd+"`。
