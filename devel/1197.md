# 1197 插件格式化

## What

使用 goldfish v18.11.23（`~/git/goldfish`）来格式化整个仓库的 Scheme（`.scm`、`.stem`）和 C++（`.cpp`、`.hpp`）文件。

## Why

goldfish v18.11.23 相比当前系统安装的 v18.11.20 有格式化规则改进，统一格式化
整个仓库确保代码风格一致，消除历史遗留的格式差异。

## How

从 `~/git/goldfish` 本地构建手动执行格式化，覆盖仓库内所有 `.scm`、`.stem`、`.cpp`、`.hpp` 文件。

（待补充具体命令和步骤）

## 涉及文件

（待补充 —— 格式化涉及的文件列表）

## 如何测试

`xmake b stem` 构建通过即可（纯格式化，无逻辑变更）。
