# [1015] 缓存推荐模板 ID 列表，支持离线展示首页推荐

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [1014.md](1014.md) - QTMHomePage 推荐模板从硬编码改为 API 请求

## 2 任务相关的代码文件
- `src/Mogan/TemplateCenter/template_cache.hpp` - 缓存管理器头文件，新增推荐 ID 缓存接口
- `src/Mogan/TemplateCenter/template_cache.cpp` - 实现 `saveRecommendIds` / `loadRecommendIds`
- `src/Mogan/TemplateCenter/template_manager.cpp` - 初始化时恢复推荐 ID，获取后持久化
- `tests/Mogan/TemplateCenter/template_cache_test.cpp` - 单元测试

## 3 如何测试

### 3.1 确定性测试（单元测试）

```bash
./bin/test_only template_cache_test
./bin/test_only template_api_parser_test
```

`template_cache_test` 覆盖：
- 推荐 ID 列表的 save/load 往返
- 顺序保持
- 空列表处理
- `clearCache()` 正确删除 `recommend_ids.json`

`template_api_parser_test` 覆盖：
- `parseTemplatesResponseOrdered()` 按原始 JSON 数组顺序返回
- 空数组返回空列表
- 非法输入返回空列表（容错）

### 3.2 非确定性测试（文档验证）

#### 首次启动（无缓存，有网络）

**前置条件**：删除缓存目录 `$TEXMACS_HOME_PATH/system/template_cache`

**验证步骤**：
1. 启动 Mogan STEM，进入启动页
2. 等待网络响应，推荐模板卡片动态出现
3. 正常退出

#### 二次启动（有缓存，无网络）

**前置条件**：正常退出后断开网络

**验证步骤**：
1. 断开网络，启动 Mogan STEM
2. 进入启动页
3. **预期**：首页立即显示上一次推荐的模板（从 `recommend_ids.json` 缓存恢复）
4. 后台仍会尝试请求，但网络失败；首页保持缓存内容不变

#### 二次启动（有缓存，有网络）

**前置条件**：正常退出后再次启动

**验证步骤**：
1. 启动 Mogan STEM，进入启动页
2. **预期**：首页立即显示上一次推荐的模板（从缓存恢复）
3. 后台发送新的推荐请求；请求回来后更新数据和 UI

## 4 提交说明

提交前执行以下最少步骤：

```bash
xmake b stem
./bin/test_only template_cache_test
./bin/test_only template_api_parser_test
```

## 5 What

1. **TemplateCache 新增推荐 ID 缓存接口**：`saveRecommendIds()` / `loadRecommendIds()`，缓存文件为 `recommend_ids.json`
2. **TemplateManager 初始化时恢复推荐列表**：`initialize()` 在加载 `metadata.json` 后，额外从 `recommend_ids.json` 恢复 `recommendTemplateIds_`
3. **TemplateManager 获取推荐后持久化**：`onRemoteRecommendTemplatesLoaded()` 在 `saveMetadataCache()` 后调用 `saveRecommendIds()`
4. **推荐模板顺序保持**：缓存以 JSON 数组形式存储，load 时按数组顺序恢复

## 6 Why

1. **离线无法展示推荐模板**：1014 中 `recommendTemplateIds_` 仅存于内存，进程退出后丢失。下次离线启动时首页推荐区为空
2. **推荐顺序丢失**：后端返回的推荐排序（如运营策略）应该保持一致，即使离线也应按上次缓存的顺序展示

## 7 How

### 7.1 缓存文件结构

`$TEXMACS_HOME_PATH/system/template_cache/recommend_ids.json`：

```json
{
  "version": "1.0",
  "recommend_ids": [
    "thu-undergraduate-thesis",
    "nsfc-ysf-c",
    "elegantbook"
  ]
}
```

### 7.2 启动恢复流程

```
[TemplateManager::initialize]
  |
  +-- loadLocalTemplates()
  +-- loadCachedCategories()
  +-- cachedMetadata = cache_->loadMetadataCache()
  +-- mergeMetadata(cachedMetadata)              ← 恢复模板元数据
  +-- recommendTemplateIds_ = cache_->loadRecommendIds()  ← 新增：恢复推荐顺序
  +-- if (!recommendTemplateIds_.isEmpty()) {
        emit recommendTemplatesLoaded();          ← 立即显示缓存的推荐卡片
      }
  +-- refreshCategories()
  +-- refreshRecommendTemplates()                  ← 每次启动都请求更新
```

### 7.3 获取后持久化流程

```
[onRemoteRecommendTemplatesLoaded]
  |
  +-- recommendTemplateIds_.clear()
  +-- for (...) recommendTemplateIds_.append(id)   ← 按 API 顺序记录
  +-- mergeMetadata(metadata, true)
  +-- cache_->saveMetadataCache(templates_)        ← 保存模板元数据
  +-- cache_->saveRecommendIds(recommendTemplateIds_)  ← 新增：保存推荐顺序
  +-- emit recommendTemplatesLoaded()
```

### 7.4 启动场景矩阵

| 场景 | 首页显示 | 后台请求 | 请求回来后 |
|---|---|---|---|
| 有缓存，有网络 | 立即显示缓存卡片 | ✅ 发送 | 更新数据 + 刷新 UI |
| 有缓存，无网络 | 立即显示缓存卡片 | ✅ 发送（失败） | 保持缓存不变 |
| 无缓存，有网络 | 只显示 new/open | ✅ 发送 | 动态添加推荐卡片 |
| 无缓存，无网络 | 只显示 new/open | ✅ 发送（失败） | 不变 |

### 7.5 关键状态变化

| 场景 | `recommendTemplatesFetched_` | `recommendTemplateIds_` |
|---|---|---|
| 首次启动，无缓存 | `false` → `true`（联网后） | 从 API 填充 |
| 二次启动，有缓存 | `false` → `true`（联网后） | 先恢复缓存，再更新 |
| 进程退出后重启 | 重置为 `false` | 从 `recommend_ids.json` 恢复 |

### 7.6 增量卡片更新

`QTMHomePage::refreshTemplateCards()` 由全量重建改为增量更新：

1. **收集现有卡片**：建立 `existingCards` 映射（ID → StyleCard 指针）
2. **计算差异**：遍历 `existingCards`，删除不在新列表中的卡片（`deleteLater`）
3. **按 API 顺序重建列表**：
   - 对于已有 ID：复用 `StyleCard` 对象（缩略图不重新下载），只更新 `loadThumbnail` 刷新 URL
   - 对于新 ID：创建新的 `StyleCard`
4. **`rearrangeStyleCards()`** 按新顺序重新布局

```cpp
StyleCard* card= existingCards.value (tmpl->id);
if (card) {
    // 复用已有卡片，避免缩略图重复下载
    newCards.append (card);
    card->loadThumbnail (tmpl->thumbnailUrl);  // 刷新 URL
}
else {
    // 新建卡片
    card= new StyleCard (style, cardsContainer_);
    ...
}
```

### 7.7 网络恢复后刷新

`TemplateManager::onNetworkStateChanged()` 在网络恢复时重置 `recommendTemplatesFetched_` 并重新请求：

```cpp
if (isOnline && initialized_) {
    refreshCategories ();
    if (recommendTemplatesFetched_) {
        recommendTemplatesFetched_= false;  // 允许重新请求
    }
    refreshRecommendTemplates ();           // 获取最新推荐
}
```

### 7.8 文件改动汇总

| 文件 | 改动 |
|---|---|
| `template_cache.hpp` | 新增 `loadRecommendIds()`、`saveRecommendIds()` 声明、`recommendIdsCachePath()` 私有方法 |
| `template_cache.cpp` | 实现上述方法，JSON 数组格式读写；`clearCache()` 补删 `recommend_ids.json` |
| `template_api.hpp` | `recommendTemplatesLoaded` 信号改为传递 `QList<TemplateMetadataPtr>`；新增 `parseTemplatesResponseOrdered()` 和 `parseTemplateObject()` 重载；两者均声明为 public（暴露给单元测试，与 `parseTemplatesResponse` 保持一致） |
| `template_api.cpp` | 实现有序解析，按 JSON 数组顺序返回；`onRecommendTemplatesReplyFinished` 调用有序解析 |
| `template_manager.hpp` | `onRemoteRecommendTemplatesLoaded` 槽参数改为 `QList` |
| `template_manager.cpp` | 初始化时恢复推荐 ID 并 emit 信号；网络恢复时刷新推荐；空数据保护前移避免清空缓存 |
| `QTMHomePage.cpp` | 增量更新推荐卡片；构造时显示已缓存推荐 |
| `template_cache_test.cpp` | 新增 4 个测试用例（save/load、空列表、顺序保持、clearCache 删除 recommend_ids）；静默消息处理器 |
| `template_api_parser_test.cpp` | 新增 3 个测试用例（有序解析顺序保持、空数组、非法输入容错） |
