# [0252] LLM Chat 模块重构：拆分、去状态化与持久化修复

## 1 相关文档
- [1076.md](1076.md) - Chat Session 性能优化
- [0194.md](0194.md) - 将 LLM 聊天模块从 dynamic 迁移到 llm 插件目录

## 2 任务相关的代码文件
- `TeXmacs/plugins/llm/progs/llm/chat-protocol.scm` — 会话协议层（新增）
- `TeXmacs/plugins/llm/progs/llm/chat-tree-ops.scm` — 无状态树操作（新增）
- `TeXmacs/plugins/llm/progs/llm/chat-tmfs.scm` — 持久化（新增，替代 chat-session-persist.scm）
- `TeXmacs/plugins/llm/progs/llm/chat-loader.scm` — 模块加载入口（新增，替代 chat-adapter.scm）
- `TeXmacs/plugins/llm/progs/llm/chat-tab-session.scm` — 已删除
- `TeXmacs/plugins/llm/progs/llm/chat-adapter.scm` — 已删除
- `src/Plugins/Qt/qt_chat_controller.cpp` — C++ 侧状态管理
- `src/Plugins/Qt/qt_chat_session.cpp` — buffer URL 与 session ID 解析
- `TeXmacs/progs/init-research.scm` — 模块加载入口

## 3 如何测试

### 3.1 确定性测试（单元测试）
```
xmake b qt_chat_session_test && xmake r qt_chat_session_test
xmake b qt_tm_widget_add_tab_test && xmake r qt_tm_widget_add_tab_test
```

### 3.2 非确定性测试（文档验证）
```
xmake b stem
# 启动 MoganSTEM → 打开 Chat tab → 发送消息 → 验证 LLM 回复正常
# 重启 → 验证历史会话恢复
```

## 4 如何提交

```bash
xmake b stem
xmake r qt_chat_session_test
gf fmt --changed-since=main
```

## 5 What

1. 将 `chat-tab-session.scm`（1300行）拆分为四个职责清晰的模块：
   - `chat-protocol.scm`：会话协议层（回调、编码解码、发送、桥接）
   - `chat-tree-ops.scm`：无状态树操作（文档处理、推理渲染、输出渲染）
   - `chat-tmfs.scm`：持久化（manifest 管理、buffer 导出/导入）
   - `chat-loader.scm`：模块加载入口（不含 import，解决 S7 环境冲突）
2. C++ 接管会话状态（model/thinking），移除 Scheme 侧 ahash-table 状态管理
3. 协议简化为单轮 `%chat`，移除多轮 `%context` 上下文构建
4. Plugin session ID 简化为 `chat-tab:{sid}`，不再包含 model 前缀
5. 持久化改用 `buffer-export` 直接写文件，移除 tmfs handler 机制
6. `chat-tab-init-session!` 改为从 C++ `getOrCreatePanel` 调用，移除幂等保护

## 6 Why

`chat-tab-session.scm` 是一个 1300 行的单体模块，混合了协议逻辑、树操作、持久化和状态管理。拆分后每个模块职责单一，便于维护和测试。同时将 Scheme 侧的 ahash-table 状态管理移到 C++ 侧（ChatSessionManager 已有完整的 CRUD），消除 Scheme/C++ 双重状态同步问题。

S7 的 `(import ...)` 与 texmacs `(use-modules ...)` 存在环境冲突：当被 `use-modules` 直接加载的模块包含 `import` 时，`tm-define` 注册到 rootlet 的函数运行时找不到 `define-expansion` 宏（如 `with-buffer`）。通过 chat-loader 无 import 入口 + chat-tab-in-buffer 内联 with-buffer 展开解决。

## 7 How

### 模块拆分策略
- `chat-tree-ops.scm`：纯函数，无 `(import ...)`，无副作用
- `chat-protocol.scm`：有 `(import (liii njson))`，包含回调、编码解码、发送逻辑
- `chat-tmfs.scm`：有 `(import (liii njson) (liii os))`，包含持久化和路径管理
- `chat-loader.scm`：无 import 的薄入口，被 `init-research.scm` 直接加载

### 去状态化
- 移除 `chat-tab-session-states`、`chat-tab-style-loaded` ahash-table
- 移除 `chat-tab-state` 构造器及访问器
- `chat-tab-send` 签名从 `(session-id)` 改为 `(session-id model thinking)`，参数由 C++ 传入
- C++ `onThinkingToggled` 直接调用 `sessionManager_.setThinking`，不再调用 Scheme

### 持久化修复
- tmfs handler 机制（`tmfs-save-handler` / `tmfs-load-handler`）未被 `buffer-save` 正确路由，导致保存失败
- 改用 main 分支的 `buffer-export` 方案：直接将 buffer 内容导出为 TMU 文件
- C++ `exportBuffer` 从 `buffer-save` 改为调用 `chat-persist-export-buffer`
- 加载时用 `tree-import` + `buffer-set-body`，不经过 tmfs

## 2026/06/09 模块拆分与持久化修复

### What
- 拆分 chat-tab-session.scm 为四个模块
- C++ 接管会话状态，移除 Scheme 侧状态管理
- 持久化改用 buffer-export，修复 tmfs save 不生效的问题
- 移除多轮上下文构建，改为单轮 %chat 协议
- 简化 plugin session ID（去掉 model 前缀）

### Why
单体模块过大且职责混乱；Scheme/C++ 双重状态同步容易出 bug；tmfs handler 机制不适用于 chat buffer 保存场景。

### How
按职责拆分模块，无 import 的 loader 作为入口避免 S7 环境冲突；用 chat-tab-in-buffer 内联 with-buffer 展开；buffer-export 替代 buffer-save + tmfs handler。
