# [0616] 任务描述

数学模式下直接敲击 `dx`, `dy`, `dz`, `dr`, `dt`, `du`, `dv`, `dw` 等时自动转换为 `<mathd>x`, `<mathd>y`, `<mathd>z`, `<mathd>r`, `<mathd>t`, `<mathd>u`, `<mathd>v`, `<mathd>w`，并且支持 `d rho`, `d theta`, `d tau`, `d upsilon`, `d phi`, `d varphi`, `d omega` 等常用微分格式。且转换后依然支持用户在中间手动输入空格或进行正常编辑。

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板

## 2 任务相关的代码文件
- [TeXmacs/progs/math/math-kbd.scm](TeXmacs/progs/math/math-kbd.scm)
- [TeXmacs/plugins/latex/progs/data/latex.scm](TeXmacs/plugins/latex/progs/data/latex.scm)
- [TeXmacs/tests/tex/0616_differential_test.tex](TeXmacs/tests/tex/0616_differential_test.tex)
- [TeXmacs/tests/0616.scm](TeXmacs/tests/0616.scm)
- [TeXmacs/tests/tex/0616_indices_test.tex](TeXmacs/tests/tex/0616_indices_test.tex)
- [TeXmacs/tests/0616_indices_test.scm](TeXmacs/tests/0616_indices_test.scm)

## 3 如何测试

### 3.1 确定性测试（单元测试）
在终端运行集成测试：
```bash
xmake r 0616
xmake r 0616_indices_test
```

### 3.2 非确定性测试（文档验证）
启动 Mogan Stem，进入数学模式：
1. 输入 `d` 再输入 `x`，验证是否自动转为 `<mathd>x`，且光标能移动到中间输入空格或进行编辑。
2. 输入 `d` 再输入 `y`，验证是否自动转为 `<mathd>y`。
3. 输入 `d` 再输入 `z`，验证是否自动转为 `<mathd>z`。
4. 输入 `d` 再输入 `r`，验证是否自动转为 `<mathd>r`。
5. 输入 `d` 再输入 `t`，验证是否自动转为 `<mathd>t`。
6. 输入 `d` 再输入 `u`，验证是否自动转为 `<mathd>u`。
7. 输入 `d` 再输入 `v`，验证是否自动转为 `<mathd>v`。
8. 输入 `d` 再输入 `w`，验证是否自动转为 `<mathd>w`。
9. 输入 `d` 再输入 `r` 和 `Tab` 键，验证是否自动转为 `<mathd><rho>`。
10. 输入 `d` 再输入 `j` 和 `Tab` 键，验证是否自动转为 `<mathd><theta>`。
11. 输入 `d` 再输入 `t` 和 `Tab` 键，验证是否自动转为 `<mathd><tau>`。
12. 输入 `d` 再输入 `u` 和 `Tab` 键，验证是否自动转为 `<mathd><upsilon>`。
13. 输入 `d` 再输入 `v` 和 `Tab` 键，验证是否自动转为 `<mathd><phi>`，再次敲击 `Tab` 键转为 `<mathd><varphi>`。
14. 输入 `d` 再输入 `w` 和 `Tab` 键，验证是否自动转为 `<mathd><omega>`。

## 4 如何提交

提交前执行以下最少步骤：
1. 检查 `TeXmacs/progs/math/math-kbd.scm` 的 Scheme 语法和括号匹配是否正确。
2. 执行 `gf fmt --changed-since=main` 格式化所有修改过的 Scheme 文件。
3. 运行集成测试 `xmake r 0616` 和 `xmake r 0616_indices_test` 确保测试全部通过。

## 5 What
1. 实现了在数学模式下连续敲击 `dx`, `dy`, `dz`, `dr`, `dt`, `du`, `dv`, `dw` 自动转换为相应的直立微分项键盘映射。
2. 实现了对希腊字母 `rho`, `theta`, `tau`, `upsilon`, `phi`, `varphi`, `omega` 微分形式的自动转换与 `Tab` 键循环。
3. 保持了字符的独立性，转换后的 `<mathd>` 与变量是两个单独的符号，用户可以直接在中间插入空格或进行自由编辑。
4. 实现了对外部 LaTeX 代码导入（包括粘贴）时的自动转换。对于形如 `dx`, `dy`, `dz`, `dr`, `dt`, `du`, `dv`, `dw` 以及希腊字母如 `d\rho`, `d\theta`, `d\tau`, `d\upsilon`, `d\phi`, `d\varphi`, `d\omega` 且中间没有空格的序列，也会在导入时被自动转换为正确的微分形式。
5. **[下标索引合并处理]** 实现了在数学模式下连续输入常用下标索引字母组合 (`ij`, `jk`, `ijk`, `kl`, `lm`, `mn`, `pq`, `rs`, `xy`, `ab`, `cd`, `uv`, `xyz`) 时自动转换为独立的斜体变量拼接（如 `i` 和 `j`），在保持 Tab 键 (`var`) 循环恢复为合并状态的同时，确保它们不会被错误合并为一个直立体单词。
6. **[LaTeX 导入下标分割]** 实现了对外部 LaTeX 导入时上述常用下标索引字母组合（包括它们乘积形式，如 `i*j` 和 `x*y*z`）的自动分割后处理，避免在 LaTeX 导入时这些组合合并为单个单词，完美还原公式原貌。

## 6 Why
此前数学模式下敲击 `dt`, `du`, `dv`, `dw` 等会被理解为多字母算子（类似 LaTeX 的 `\mathrm{du}`），不便输入和修改。外部 LaTeX 导入时这些也会被解析为普通字符，导致排版不正确。本次改动延续了之前常用微分项的智能快捷纠正与完美的外部代码导入转换逻辑，并将该支持扩展到了 `t`, `u`, `v`, `w` 参数及常见其对应的希腊字母形式（包括新增 `t` 和 `\tau`），提供了绝佳的用户体验。

此外，当用户输入或导入类似 `ij`, `jk` 的常用下标索引时，由于它们在数学模式中通常代表两个独立的标量索引（即代表乘积 $i \times j$ ），如果被渲染合并为单个直立单词（类似单算子词），则严重不符合数理排版规范。通过自动对这些常用索引组合进行自动分割（无论是按键输入还是 LaTeX 导入），能够大幅提高数学公式排版专业度。

## 7 How
1. 在 `TeXmacs/progs/math/math-kbd.scm` 的 `(:mode in-math-not-hybrid?)` 快捷键块中增加以下按键序列映射：
   - `("d t" "<mathd>t")`
   - `("d t var" "<mathd><tau>")`
   - `("d t var var" "dt")`
    - 增加常用索引组合映射，例如：`("i j" (insert '(concat "i" " " "j")))`, `("i j var" "ij")` 等，覆盖 13 种常见双字母和三字母索引组合。

2. 在 LaTeX 插件重构后，由于旧的 `init-latex.scm` 被删除，我们在 `TeXmacs/plugins/latex/progs/data/latex.scm` 中对 `latex->texmacs` 和 `latex-document->texmacs` 的后处理流程进行了重构和扩展：
   - 扩展 `match-differential` 函数：增加 `"t"`, `"u"`, `"v"`, `"w"` 字母前缀匹配，并支持匹配 `<tau>`, `<upsilon>`, `<phi>`, `<varphi>`, `<omega>` 等希腊符号，以支持 `dt`, `du`, `dv`, `dw` 等微分格式。
   - 扩展 `transform-concat-children` 函数中的希腊字母匹配：加入 `<tau>`, `<upsilon>`, `<phi>`, `<varphi>`, `<omega>`，当解析到的 `concat` 子节点结尾为 `"d"` 且下一节点为上述希腊字母时，自动将其切分为带空格的直立微分形式。
    - 新增 `match-math-index-combination` 函数：用于专门检测和匹配包括 `i*j` 和 `ij` 以及 `xyz` 在内的 13 种常见索引组合，并验证其前后的词边界。
   - 扩展 `transform-math-string` 函数：在 `else` 分支的首要位置优先调用 `match-math-index-combination`。如果匹配成功，则直接将字符串分割为对应的各个单字母元素并存入 `res`，然后跳过对应长度并继续循环，从而优雅地实现对 LaTeX 导入数学索引的完美分割后处理。
