# [0775] 远端执行操作时设 user_active 为 false 防止触发滚动
## What
使用了类似 LLM 插件的做法，在远端执行操作时设 user_active 为 false 防止触发滚动到当前光标位置

## 如何测试
```
xmake f --loro=yes
xmake b stem
xmake r stem
```
在协同编辑中，写一篇比较长的文档。

在 A 中，把光标移动到文档末尾，然后可视区域滚动到文档顶部
在 B 中，进行文档修改。

A 不自动滚动到光标所在位置。
