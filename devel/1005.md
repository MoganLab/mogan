# [1005] 模板打开时保存到 Documents 文件夹

## 相关文档
- [dddd.md](dddd.md) - 任务文档模板

## 任务相关的代码文件
- `src/Plugins/Qt/qt_template_utils.hpp`
- `src/Plugins/Qt/qt_template_utils.cpp`
- `src/Plugins/Qt/qt_home_page.cpp`
- `src/Plugins/Qt/qt_template_page.cpp`

## 如何测试

### 非确定性测试（功能验证）
1. 启动 Mogan，进入 Template Center
2. 点击一个**已缓存**的模板卡片 → 应正常打开，且 Documents 文件夹下生成对应 `.tmu` 文件（如 `TemplateName.tmu`）
3. 再次打开同一模板 → 应生成带序号的文件（如 `TemplateName(1).tmu`），不覆盖之前文件
4. 删除本地缓存后点击模板 → 下载完成后应同样拷贝到 Documents 再打开
5. Home 页只保留 "New document" 和 "Open document"，固定模板入口已暂时移除（首次无缓存下载会触发crash，后面修复）

## 如何提交

提交前执行以下最少步骤：

```bash
xmake build
```

## What

1. **新建 `qt_template_utils` 工具模块**：提取模板拷贝和文档加载的公共逻辑，供 Home 页和 Template Center 页复用。
2. **本地已缓存模板**：打开时先拷贝到用户 Documents 文件夹，再执行 `load-document`，避免直接编辑缓存文件。
3. **首次下载模板**：下载完成后同样拷贝到 Documents 再打开，保持行为一致。
4. **自动编号去重**：Documents 中已存在同名文件时，自动生成 `Name(1).tmu`、`Name(2).tmu` 等序号文件名。
5. **暂时移除 Home 页固定模板入口**：`elegantbook` 和 `nsfc-ysf-c` 两个固定卡片已注释掉，因异步下载完成后在模态对话框 cleanup 期间调用 `eval_scheme` 会触发 `no window attached to view` 崩溃，待后续修复后再启用。

## Why

- 用户直接打开模板时，原先会直接加载缓存目录下的 `.tmu` 文件，导致用户编辑的内容污染缓存。
- 需要保证每次打开模板都是一份全新的副本，同时保留原始缓存不被修改。
- 两个页面（Home 页、Template Center 页）存在大量重复的"拷贝+加载"代码，需要提取复用。

## How

### 1. 工具函数提取

新增 `qt_template_utils.hpp/cpp`，提供四个无状态工具函数：

- `qt_generate_document_save_path`：基于 `QStandardPaths::DocumentsLocation` 生成唯一文件名，自动过滤非法字符、自动编号。
- `qt_copy_template_to_documents`：调用生成函数得到目标路径，执行 `QFile::copy`。
- `qt_load_document_path`：封装 `eval_scheme("(load-document ...)")`。
- `qt_copy_template_and_load`：组合拷贝+加载，失败时通过 `QtFloatingToast` 提示用户。

### 2. 使用方改造

`qt_home_page.cpp` 和 `qt_template_page.cpp` 中，本地缓存分支和下载完成回调统一替换为：

```cpp
// 本地已有缓存
qt_copy_template_and_load(this, localPath, meta->name);

// 下载完成回调（meta 缺失时回退到直接加载缓存）
if (!meta) {
    qt_load_document_path(localPath);
    return;
}
qt_copy_template_and_load(this, localPath, meta->name);
```

### 3. 已知问题与规避

Home 页中 `elegantbook`/`nsfc-ysf-c` 固定模板在**首次下载**完成后，从 `downloadCompleted` 信号槽中调用 `eval_scheme` 会触发 Mogan 内部断言 `no window attached to view`。原因：模态 `QProgressDialog` 关闭后，Scheme 层 `concrete_window()` 找不到当前激活窗口。该问题仅影响从 Home 页触发的异步下载路径，Template Center 页暂未发现同样问题。为不影响主线，暂时移除 Home 页这两个固定模板入口。
