# [2021] 用 QML 模态对话框重写「是否保存」确认框

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [2014.md](2014.md) - GUI 自动化复现 + 异步链驱动测试的方法论（本任务沿用）
- [1106.md](1106.md) - GUI 集成测试 `MOGAN_TEST_GUI` 机制说明

## 2 任务相关的代码文件
- `src/Plugins/Qt/QTMQmlDialog.cpp` / `.hpp` - QML 模态对话框底座 `qt_show_qml_dialog` + confirm-close 入口 `cpp_confirm_close`
- `src/Plugins/Qt/QTMQmlDialogBridge.hpp` - 暴露给 QML 的桥对象（Q_OBJECT，单独成头便于 moc 扫描）
- `src/Plugins/Qt/qml/ConfirmClose.qml` - 「是否保存」弹窗视觉（自绘圆角 + 深浅色跟随主题）
- `src/Plugins/Qt/moganqml.qrc` - QML 资源文件
- `src/Plugins/Qt/QTMTabPage.cpp` / `.hpp` - `cpp_kill_tabpage`（修正 `g_mostRecentlyClosedTab` 标记时机）+ 关闭按钮 hover 时机修正
- `src/Texmacs/Window/tm_dialogue.cpp` - `choose-file` 测试钩子（`MOGAN_TEST_CHOOSE_FILE`）
- `TeXmacs/progs/texmacs/texmacs/tm-server.scm` - `confirm-close-dialog` 改调 `cpp-confirm-close`；`safely-kill-tabpage*` 改调 `cpp-kill-tabpage`
- `TeXmacs/progs/prog/glue-symbols.scm` - 注册 `cpp-confirm-close`
- `src/Scheme/Glue/glue_basic.lua` / `src/Scheme/L5/glue_l5_extra.hpp` - 注册 `cpp-kill-tabpage` / `cpp-confirm-close`
- `xmake.lua` / `xmake/tests.lua` - 加 `QtQuickWidgets` framework
- `tests/Plugins/Qt/qt_qml_dialog_test.cpp` - confirm-close 纯逻辑契约单元测试（新增）
- `tests/Plugins/Qt/qtquick_test.cpp` - 验证 `QQuickWidget`（QtQuickWidgets）可链接
- `TeXmacs/tests/2021.scm` + `TeXmacs/tests/tmu/2021.tmu` - GUI 集成测试脚本与夹具

## 3 如何测试

### 3.1 确定性测试（单元测试）
```bash
xmake b qt_qml_dialog_test && xmake r qt_qml_dialog_test   # confirm-close 纯逻辑契约
xmake b qtquick_test && xmake r qtquick_test               # QtQuickWidgets 可链接
```

`qt_qml_dialog_test` 通过 `MOGAN_TEST_CONFIRM_CLOSE` 钩子绕过模态 `exec()`，覆盖：
- 钩子命中三选一（Save / Don't save / Cancel）的返回值映射；
- `scratch=true` 时肯定按钮语义为「Save as」（返回值仍是 "Save"，由 scheme 侧据此走另存为）；
- 钩子为非法值时不命中。

### 3.2 非确定性测试（GUI 集成）
```bash
xmake b stem
MOGAN_TEST_GUI=1 xmake r 2021
```

`2021.scm` 用异步链驱动真实 GUI，覆盖普通文档 / scratch 文档的 Cancel、Don't save、Save 分支，
回调触发契约，以及 `confirm-close-dialog` 的 `opt-buffer` 参数。两个钩子：
- `MOGAN_TEST_CONFIRM_CLOSE` 控制 `cpp-confirm-close` 返回值；
- `MOGAN_TEST_CHOOSE_FILE` 控制 `choose-file` 返回路径（scratch 另存为用）。

> 注意：`2021.scm` 顶部 `(load ".../tm-server.scm")` 不能删——`confirm-close-dialog`
> 是普通 `define`（非 `tm-define`，未进 glue），测试进程需显式 load 才能拿到其定义。

## 4 如何提交

提交前执行以下最少步骤：

```bash
gf fmt --changed-since=main          # 格式化变更的 .scm / .cpp / .hpp
xmake b stem                          # 主项目构建
xmake b qt_qml_dialog_test && xmake r qt_qml_dialog_test
MOGAN_TEST_GUI=1 xmake r 2021         # GUI 集成测试（24 checks）
```

按 CLAUDE.md：第一个 commit 更新本任务文档，后续 commit 为代码改动；提交信息格式 `[2021] 简述`。

## 5 What

1. 新增 QML 模态对话框通用底座 `qt_show_qml_dialog`（无边框透明 `QDialog` + `QQuickWidget`，
   `exec()` 阻塞返回按钮下标），并为「确认关闭」实现具体入口 `cpp_confirm_close`。
2. 用 QML 自绘弹窗（`ConfirmClose.qml`，圆角 + 深浅色）替换原 `tm-widget` 实现的
   `confirm-close-widget`；`confirm-close-dialog` 改为同步调用 `cpp-confirm-close`。
3. 新增 `cpp_kill_tabpage`，把 `g_mostRecentlyClosedTab` 的标记时机从「关闭按钮点击时」
   后移到「真正 kill 之前」，修复「点取消后 tab 仍被错误隐藏」；关闭按钮在弹窗期间隐藏
   高亮，取消后按鼠标位置恢复。
4. 新增 `cpp-kill-tabpage` / `cpp-confirm-close` glue；构建加 `QtQuickWidgets` framework
   与 `moganqml.qrc` 资源。
5. 补 `qt_qml_dialog_test.cpp`（confirm-close 纯逻辑契约）与 `2021.scm` GUI 集成测试。

## 6 Why

原 `tm-widget` + `dialogue-window` 的异步机制有两个顽疾：
- **连点重复弹窗**：快速多次点关闭按钮，异步对话框并发弹出多个。
- **X 关闭后无法二次弹出**：异步对话框生命周期与触发动作解耦，关闭一次后状态错乱。

根因是「异步 + 非串行化」。改用同步模态 `QDialog::exec()`，阻塞直到用户选择，天然把连点
请求串行化，从根上消除上述两类问题。同时 QML 弹窗视觉更现代，并为后续从「Qt widget 导出
scheme」的旧弹窗机制逐步迁移到 QML 提供通用底座。

## 7 How

- **串行化**：`qt_show_qml_dialog` 内部跑一个无边框本地模态 `QDialog`（包一个加载了指定
  QML 的 `QQuickWidget`），`exec()` 阻塞返回用户点选的按钮下标（取消 / X / Esc → -1）。
  `exec()` 的模态特性天然串行化连点请求。
- **跨层接口**：正文与按钮文案由调用方（scheme 侧算好经 glue 传入）提供，i18n 走既有
  `qt_translate` 机制（与原 scheme 路径一致）。桥对象 `QmlDialogBridge` 以 `QDialog` 为
  parent，QML 按钮点击 / 拖窗以 `choose(index)` / `startMove()` 反馈回 C++，`QDialog`
  析构自动释放，无泄漏。
- **三重锁尺寸**：QML root 用 `anchors.fill` 无固定 implicit 尺寸，`QQuickWidget` 的
  sizeHint 会塌缩，导致 `exec()` 时窗口被压到约 100×30。用 `qw->setFixedSize` +
  `QLayout::SetFixedSize` + `d.setFixedSize` 三重锁定为 `DpiUtils::scaled(400×200)`。
- **透明视口**：用 `QQuickWidget::setClearColor(transparent)` 而非 `WA_TranslucentBackground`
  （后者对 `QQuickWidget` 不完全生效，默认白底会盖住 `QDialog` 透明，露出方角）。
- **标记时机修正**：原实现把 `g_mostRecentlyClosedTab=...` 写在关闭按钮的 clicked 回调里，
  早于 `confirm-close-dialog`。点取消时 kill 未发生但标记已设，导致后续刷新错误隐藏 tab。
  `cpp_kill_tabpage` 把赋值挪到 `kill_tabpage` 调用紧前，保证「只有真关闭才标记」。
