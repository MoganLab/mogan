# [1125] 为 cork_to_utf8 与 utf8_to_cork 撰写完整测试用例

## 相关文档
- [1113.md](1113.md) - Herk 编码重新实现与测试（本任务的姊妹任务）
- [66_13.md](66_13.md) - Herk 编码的设计与转换规则
- [dddd.md](dddd.md) - 任务文档模板

## 任务相关的代码文件
- `src/Data/String/converter.hpp` - `cork_to_utf8` / `utf8_to_cork` 声明
- `src/Data/String/converter.cpp` - 上述函数的实现（现已转发到 lolly），以及 `converter_rep::load()` 加载映射表
- `tests/Data/String/converter_test.cpp` - 测试文件（覆盖 0x00–0xFF 全字节 + 命名实体 + 往返）
- `lolly/lolly/data/cork.hpp` / `lolly/lolly/data/cork.cpp` - 迁移后的 `utf8_to_cork` / `cork_to_utf8` / `strict_cork_to_utf8` 实现（数据驱动，不再依赖 .scm 运行时加载）
- `lolly/lolly/data/cork_data.inc` - 自动生成的静态映射数据（从 .scm 字典编译期固化）
- `lolly/tests/lolly/data/cork_test.cpp` - lolly 侧白盒单元测试（doctest）
- `TeXmacs/tests/1125.scm` - 性能基准（用 `(liii timeit)` 测三函数在 ASCII/Latin-1/CJK/命名实体/混合语料下的耗时）
- `TeXmacs/langs/encoding/corktounicode.scm` - Cork↔Unicode 双向映射主表
- `TeXmacs/langs/encoding/cork-unicode-oneway.scm` - Cork→Unicode 单向映射（0x18/0x1A 等）
- `TeXmacs/langs/encoding/unicode-cork-oneway.scm` - Unicode→Cork 单向映射
- `TeXmacs/langs/encoding/tmuniversaltounicode.scm` - `<less>`/`<gtr>` 等命名实体定义

## 如何测试
```bash
xmake b converter_test
xmake r converter_test
```

### 预期输出
```
********* Start testing of TestConverter *********
Config: ……
PASS   : TestConverter::initTestCase()
PASS   : TestConverter::test_utf8_to_cork_0x()
...（按高半字节分组的多个用例）
PASS   : TestConverter::test_cork_to_utf8_escapes()
PASS   : TestConverter::cleanupTestCase()
Totals: N passed, 0 failed, ...
********* Finished testing of TestConverter *********
```

## What

为 `cork_to_utf8` 与 `utf8_to_cork` 补充覆盖全部 0x00–0xFF 字节区间的完整单元测试，
按高半字节（0x/1x/2x/.../Fx）分组组织，风格参照 `lolly/tests/lolly/data/herk_test.cpp`。

同时补充以下专项用例：
- `<#XXXX>` 十六进制转义（短/长形式、Cork 已映射码点、Cork 未映射的 CJK/Emoji）
- 命名实体（`<less>` / `<gtr>` / `<comma>` / `<grave>` / `<varspace>` 等文本类，
  以及希腊字母、二元运算、关系、箭头、集合论、微积分、点/撇号、特殊字母形等
  数学符号类别，覆盖 `tmuniversaltounicode.scm` 的 929 个单码点实体中各类代表）
- 混合字符串（Cork 字节 + 转义 + 命名实体交错）
- 往返一致性（`utf8_to_cork ∘ cork_to_utf8` 在可逆区间上保持不变）

## Why

`cork_to_utf8` 与 `utf8_to_cork` 是 TeXmacs 文档读写、剪贴板交互、Scheme 求值等链路上的
核心编解码函数。此前 `tests/Data/String/converter_test.cpp` 仅有 3 条用例，覆盖极薄，
既无法回归保护 0x00–0xFF 全表的正确性，也无法暴露单向映射、命名实体等边界行为。

本任务**仅新增测试**，不修改 `converter.cpp` / 映射表 / `.scm` 文件中的任何代码行为。
测试的预期值一律以当前运行时实际行为为准（已通过临时探针逐一确认），而非以任何"理想"
或"文档承诺"的语义为准——这样测试锁定的是现状，未来若有人改动转换逻辑，这些用例能
真实反映行为变化。

## How
1. 用临时 `test_probe` 槽（事后删除）逐一确认 0x18/0x1A/0x3C/0x3E/0x60/0x7F 及命名实体、
   往返路径的运行时输出（见下文"迷惑行为"小节）。
2. 按 `corktounicode.scm` 主表，为 0x00–0xFF 中**有直接单字节映射**的字节编写
   `cork_to_utf8_Nx` / `utf8_to_cork_Nx` 用例（跳过 0x18/0x1A/0x3C/0x3E/0xDF 等
   无直接映射或仅多字节序列映射的字节）。
3. 编写转义、命名实体、混合、往返等专项用例。
4. 删除临时 `test_probe` 槽。
5. `xmake b converter_test && xmake r converter_test` 全绿后提交。

## 迷惑行为记录（运行时实测，本次不改）

以下行为是当前实现的**实际输出**，测试用例会如实锁定它们。它们大多源于
`converter_rep::match()` 的最长前缀匹配 + `copy_unmatched` 语义，以及 Cork↔Unicode
映射表本身的非对称性。是否应修正留待后续任务讨论。

下表是通过遍历全部 0x00–0xFF 字节、做 `utf8_to_cork(cork_to_utf8(b))` 往返后实测得到的，
**非往返的字节只有 5 个**：0x18、0x1A、0x3C、0x3E、0xDF。其余 251 个字节均可往返。

补充扫描了全部 U+0000–U+00FF 码点的 `utf8_to_cork` 方向，**没有任何码点被静默丢弃**
（无映射且码点 < 256 的会通过 `copy_unmatched` 直通为对应字节，码点 ≥ 256 的会转义为
`<#XXXX>` 或命名实体）。

### 1. Cork 0x60 ↔ U+2018，而 U+0060 ↔ Cork 0x00（两套不同的映射）
- `cork_to_utf8(byte 0x60)` → U+2018 (`‘`，左单引号)，反向 `utf8_to_cork("‘")` → 0x60 ✓
- `utf8_to_cork("`")` (U+0060 backtick) → Cork byte 0x00，反向 `cork_to_utf8(byte 0x00)` → U+0060 ✓

> 即：Cork 字节 0x60 与 Unicode U+2018 互为映射；Unicode U+0060（ASCII backtick）则
> 与 Cork 字节 0x00 互为映射。两套映射各自可逆，不要混淆。

### 2. Cork 0x7F ↔ U+2010（可往返）
- `cork_to_utf8(byte 0x7F)` → U+2010 (`‐`，hyphen)，反向 `utf8_to_cork("‐")` → 0x7F ✓

### 3. Cork 0x18 / 0x1A：单向解码到 ASCII（非往返）
- `cork_to_utf8(byte 0x18)` → `"0"`（来自 `cork-unicode-oneway.scm` 的 perthousand-zero）
- `cork_to_utf8(byte 0x1A)` → `"j"`（来自同文件的 dotless-j）
- `utf8_to_cork("0")` → ASCII `"0"` (0x30，**不是** Cork 0x18)
- `utf8_to_cork("j")` → ASCII `"j"` (0x6A，**不是** Cork 0x1A)

> 即：Cork 0x18/0x1A 解码到 ASCII 0/j，但编码方向 ASCII 0/j 回到的是普通 ASCII 字节，
> 往返不可逆。

### 4. 单字节 `<` / `>` 往返后变成命名实体（非往返）
- `cork_to_utf8("<")` → `"<"`（U+003C，直通；hashtree 把 `<` 当作 `<less>` 前缀，
  作为未完成匹配回退，输出字面 `<`）
- `cork_to_utf8(">")` → `">"`（同上）
- `utf8_to_cork("<")` → `"<less>"`
- `utf8_to_cork(">")` → `"<gtr>"`
- 往返：`utf8_to_cork(cork_to_utf8("<"))` → `"<less>"`（单字节 `<` 变成 6 字节 `<less>`）

### 5. Cork 0xDF（sharp s）单向解码到 "SS"（非往返）
- `cork_to_utf8(byte 0xDF)` → `"SS"`（来自 `cork-unicode-oneway.scm`）
- `utf8_to_cork("SS")` → `"SS"`（两个 ASCII S，**不是** Cork 0xDF）
- 但 `utf8_to_cork("ß")` (U+00DF) → Cork byte 0xFF，反向 `cork_to_utf8(byte 0xFF)` → U+00DF ✓
  （Cork 0xDF 与 U+00DF 是不同的映射路径）

### 6. `<comma>` 与字面 `,` 解码结果相同
- `cork_to_utf8("<comma>")` → `","`
- `cork_to_utf8(",")` → `","`
- `utf8_to_cork(",")` → `","`（ASCII 字节，**不是** `<comma>`）

> `<comma>` 命名实体仅在 Cork→Unicode 方向有意义（来自 `cork-unicode-oneway.scm`），
> 用于在需要区分"十进制逗号"与普通逗号的上下文中保留语义；但编码方向无法区分。

### 7. `<grave>` 命名实体在 `cork_to_utf8` 中产生 backtick
- `cork_to_utf8("<grave>")` → `` "`" `` (U+0060)
- `utf8_to_cork("`")` → Cork byte 0x00（见第 1 点，不是 0x60）

> `<grave>` 的定义见 `symbol-unicode-math.scm`。

### 8. Cork 0x17 = U+2060（WORD JOINER），不是 U+200B
- `cork_to_utf8(byte 0x17)` → U+2060 (WORD JOINER, `\xE2\x81\xA0`)
- 注意 herk 的 0x17 对应 U+200B (ZWSP)，二者不同。Cork 用 U+2060 表达"不换行"语义。

### 9. U+2019（右单引号）映射到 Cork 0x27，不是转义
- `utf8_to_cork("’")` (U+2019) → Cork byte 0x27（与 ASCII apostrophe `'` 共用同一字节）
- 来自 `unicode-cork-oneway.scm` 的 `"#2019" "'"` 条目

### 10. U+00A0（不换行空格）映射到命名实体 `<varspace>`
- `utf8_to_cork("\xC2\xA0")` → `"<varspace>"`
- 来自 `unicode-cork-oneway.scm` 的 `"#00A0" " "` 与命名实体结合

### 11. 命名实体 `<epsilon>` / `<varepsilon>`、`<phi>` / `<varphi>` 的变体互换
通过遍历 `tmuniversaltounicode.scm` 全部 929 个单码点命名实体实测，绝大多数实体的
`cork_to_utf8("<name>")` 输出与 `.scm` 声明的码点一致（928/929），仅以下两组的
**plain 与 variant 互换**：
- `<epsilon>` → U+03F5 (`\xCF\xB5`，lunate epsilon)，而 `<varepsilon>` → U+03B5 (`\xCE\xB5`，plain)
- `<phi>` → U+03D5 (`\xCF\x95`)，而 `<varphi>` → U+03C6 (`\xCF\x86`)

> 即：TeXmacs 把"普通"希腊 epsilon/phi 映射到 Unicode 的 variant 码点，反之亦然，
> 与常见约定相反。测试如实锁定这一行为。

### 12. 命名实体解码后部分不往返回原名（Cork 字节优先 / canonical name 不同）
遍历 929 个实体的 `utf8_to_cork(cork_to_utf8("<name>"))` 往返，**仅 7 个不回到原名**：
- `<sterling>` → U+00A3 → Cork byte 0xBF（£ 有直接字节映射，优先于命名实体）
- `<guillemotleft>` → U+00AB → Cork byte 0x13（« 同理）
- `<guillemotright>` → U+00BB → Cork byte 0x14（» 同理）
- `<sum>` → U+2211 → `"<big-sum>"`（canonical 反向名不同）
- `<hbar>` → U+210F → `"<hslash>"`
- `<Re>` → U+211C → `"<frak-R>"`
- `<Im>` → U+2111 → `"<frak-I>"`

> 其余 922 个命名实体均往返回到自身。`<sterling>`/`<guillemot*>` 的不往返与前文
> 第 4 点（单字节 `<>` 不往返）同源：某些 Unicode 码点在 Cork 里有直接字节表示，
> 编码方向优先使用字节而非命名实体。

### 13. 部分内部宏名不被 `cork_to_utf8` 识别为命名实体
`<le>` / `<ge>` / `<to>` / `<oplus>` / `<imath>` / `<jmath>` / `<dag>` / `<permil>` 等
不在 `tmuniversaltounicode.scm` 主表中（多为 TeXmacs 内部宏或被注释），`cork_to_utf8`
对它们**不识别**，按字面 `<` 开头处理（受 `<less>` 前缀匹配逻辑影响）。测试不覆盖
这些不被识别的名字。

## 非目标（本次不做）
- 不修改 `converter.cpp`、`.scm` 映射表、`converter_rep::load()` 的任何行为。
- 不修复上述迷惑行为（是否修正、如何修正留待后续任务）。
- 不迁移到 lolly（Cork 工具迁移已在 [1113.md](1113.md) 后置任务中规划）。

整理时间：2026/06/27

---

## 后续：把 `utf8_to_cork` / `cork_to_utf8` / `strict_cork_to_utf8` 迁到 lolly（2026-06-27）

### What
参照 [1113.md](1113.md) Herk 的迁移模式，把 `src/Data/String/converter.cpp` 里的三个
Cork↔UTF-8 转换函数迁到 `lolly/lolly/data/cork.{hpp,cpp}`：

- 函数签名不变（仍然是 `string utf8_to_cork(string)` / `string_u8 cork_to_utf8(string)` /
  `string_u8 strict_cork_to_utf8(string)`），只是包到 `lolly::data` 命名空间里。
- `src/Data/String/converter.cpp` 里同名全局函数保留，作为薄转发：
  ```cpp
  string       utf8_to_cork         (string input) { return lolly::data::utf8_to_cork (input); }
  string_u8    cork_to_utf8         (string input) { return lolly::data::cork_to_utf8 (input); }
  string_u8    strict_cork_to_utf8  (string input) { return lolly::data::strict_cork_to_utf8 (input); }
  ```
  外部所有调用点（约 30 处）不需要改。
- 数据来源从运行时加载 `.scm` 字典改为编译期固化的静态表 `cork_data.inc`，由
  `/tmp/gen_cork_cpp.py`（一次性脚本，不入库）从同样的 4–6 个 `.scm` 字典生成。

### Why
- **可读性 / 可移植性**：lolly 是底层库，不应依赖 TeXmacs 的资源文件路径
  （`$TEXMACS_PATH/langs/encoding/*.scm`）。Herk 已经完成同样的迁移，Cork 是它的姊妹任务。
- **性能**：原实现每次转换走 `hashtree<char, string>` 的最长前缀匹配（每次 `apply` 都从
  root 起步逐字节 `contains`），ASCII 字符也要走一遍 trie。新实现：
    - `utf8_to_cork`：ASCII 直接索引 `[128]` 表；多字节 codepoint 二分一张 1092 条的排序表。
    - `cork_to_utf8`：1 字节 cork 直接索引 `[256]` 表；`<name>` 命名实体二分 1187 条排序表；
      `<#XXXX>` 转义直接走 `from_hex` + `encode_as_utf8`；3 条非实体多字节 key
      （`%\x18` / `%\x18\x18` / `...`）线性探测。
- **首启开销**：原 `load_converter` 第一次调用要读 6 个 `.scm` 文件 + 解析 scheme + 建
  hashtree；新实现零启动开销，数据在 `.rodata` 段。

### 行为等价性
迁移前后的 `tests/Data/String/converter_test.cpp`（70 个用例，覆盖 0x00–0xFF 全字节、
`<#XXXX>` 转义、命名实体、混合字符串、往返一致性、`strict_cork_to_utf8` fallback 排除等）
全部 PASS，**包括本文件上方记录的 13 条"迷惑行为"**——新实现忠实复现了这些非直觉的
现行行为（例如 U+00A0 → `<varspace>` 而非空格、Cork 0x60 ↔ U+2018、`<`/`>` 单字节不
往返等）。`devel/1125.md` 中的"迷惑行为记录"是测试的预期值来源。

实现这些等价性的几个关键点：
1. **last-wins 语义**：原 `put_prefix_code -> set_label` 总是覆盖；同 codepoint 在多个
   字典里出现时（如 U+00A0 既在 `unicode-cork-oneway` 也在 `tmuniversaltounicode`），
   后加载的字典覆盖前者。生成器用 `dict[key] = value`（不是 `setdefault`）。
2. **`copy_unmatched` 语义**：1 字节 cork key 无映射（只有 0x3C `<` / 0x3E `>`）时，
   按字节直通；对应 utf8 方向的 ASCII 控制字符（0x00–0x1F + 0x7F）也直通。
3. **invalid UTF-8**：lolly 的 `decode_from_utf8` 对孤立的 0x80 / 0xC3 等"萎缩"为
   单字节 codepoint；这些走 1-byte 表（无映射 → 直通），不进入 `<#XXXX>` 转义分支
   （只有 `code >= 256` 才转义）。
4. **`<#XXXX>` 优先级最高**：在 `cork_to_utf8` 中，遇到 `<#` 直接转义，不再尝试命名
   实体；其余 `<...>` 才查实体表。
5. **长前缀优先**：3 条非实体多字节 key 必须在 1-byte 表之前尝试，否则 `%\x18` 会
   被错误地拆成 `%` + `\x18` 两个单字节。

### 性能对比
同一份 Scheme 基准 `TeXmacs/tests/1125.scm`（用 `(liii timeit)` 跑 50000 次迭代，
5 种语料 × 3 函数 = 13 组测量），迁移前后各跑一次：

| 函数 / 语料           | 迁移前 (s) | 迁移后 (s) | 提速    |
| --------------------- | ----------:| ----------:| -------:|
| utf8→cork   ascii     | 1.05       | 0.23       | **4.6×**|
| utf8→cork   latin1    | 0.78       | 0.21       | **3.7×**|
| utf8→cork   cjk       | 2.21       | 0.96       | **2.3×**|
| utf8→cork   named     | 2.63       | 0.64       | **4.1×**|
| utf8→cork   mixed     | 0.63       | 0.19       | **3.3×**|
| cork→utf8   latin1    | 0.62       | 0.19       | **3.3×**|
| cork→utf8   cjk       | 0.71       | 0.35       | **2.0×**|
| cork→utf8   named     | 2.11       | 0.78       | **2.7×**|
| cork→utf8   mixed     | 0.34       | 0.13       | **2.6×**|
| strict→utf8 latin1    | 0.68       | 0.19       | **3.6×**|
| strict→utf8 cjk       | 0.75       | 0.35       | **2.1×**|
| strict→utf8 named     | 2.22       | 0.82       | **2.7×**|
| strict→utf8 mixed     | 0.34       | 0.14       | **2.4×**|

平均约 3× 提速；最慢路径（CJK 转义、命名实体解码）也从 ~2.2s 降到 ~0.8s。

### How
1. 写 `TeXmacs/tests/1125.scm`，用 `(liii timeit)` 基准 5 种语料 × 3 函数。
   `xmake b 1125 && xmake r 1125`，保存"迁移前"基线。
2. 写 Python 生成器 `/tmp/gen_cork_cpp.py`：解析 6 个 `.scm` 字典，按 `converter_rep::load()`
   的方向（reverse / escape 组合）合成映射，输出 `cork_data.inc`（static const 数组 +
   平行的字节池 + slice `{off, len}`）。
3. 写 `lolly/lolly/data/cork.{hpp,cpp}`：声明放在公开头（参照 `herk.hpp`），实现含
   `utf8_to_cork_cp_index` / `cork_to_utf8_entity_at` / `strict_cork_to_utf8_entity_at` /
   `cork_to_utf8_special_at` 等二分查找 helper，以及 3 个公开函数。
4. `src/Data/String/converter.cpp` 的三个旧实现替换为转发到 `lolly::data::*`。
5. 跑 `xmake b converter_test && xmake r converter_test`——必须 70/70 PASS（即本文档上方
   锁定的全部行为）。
6. 再跑 `xmake r 1125`，对比提速。
7. 写 `lolly/tests/lolly/data/cork_test.cpp`（doctest 白盒测试）覆盖关键路径
   （注：当前需在 lolly 子项目里 `xmake b lolly_tests` 才能跑，mogan 主项目尚无 target
   暴露它；converter_test 的 70 个用例已是充分的回归保护）。

### 非目标（本次不做）
- 不修改任何 `.scm` 字典内容。
- 不修复"迷惑行为"——测试如实锁定现状。
- 不删 `converter_rep::load()` / `apply()` / hashtree 路径（其它编码如 T2A、HTML、LaTeX
  仍在用）。
- 不迁移 `sourcecode_to_cork` / `cork_to_sourcecode` / T2A / LaTeX 等其它方向。

### 后续：把 converter_test.cpp 的完整测试迁到 cork_test.cpp（2026-06-27）

#### What
- `tests/Data/String/converter_test.cpp` 删除。
- 内容迁移到 `lolly/tests/lolly/data/cork_test.cpp`（doctest 风格，参照
  `herk_test.cpp`），68 个 `TEST_CASE`、917 个 `string_eq` 调用，覆盖范围与原文件
  完全一致（0x00–0xFF 全字节、`<#XXXX>` 转义、命名实体、混合、往返、strict fallback
  排除、invalid UTF-8 等）。
- 转换由一次性脚本 `/tmp/convert_test.py` 完成（不入库）：
  - `void\nTestConverter::test_xxx () {`  →  `TEST_CASE ("xxx") {`
  - `qcompare (a, b);` / `QCOMPARE (a, b);`  →  `string_eq (a, b);`
  - 去掉 QtTest 专属的 `Q_OBJECT` / `init` / `cleanup` slot / `QTEST_MAIN` / `.moc`。
  - 保留 `cork_byte (int)` helper、所有 UTF-8 字面量、原文件的所有注释和分节标题。

#### Why
- converter_test.cpp 的所有用例都是 cork 相关（70 个 slots 里 68 个是 cork 用例，2 个
  是 QtTest 的 init/cleanup）。函数已经迁到 lolly，测试跟过去更自然。
- 跟 `herk_test.cpp` 的位置/风格保持一致：实现和测试同在 `lolly/lolly/data/` 和
  `lolly/tests/lolly/data/` 下。
- doctest 比 QtTest 更轻量（无 moc、无 QObject 子类、无 QTEST_MAIN），跟 lolly 子项目
  其它测试一致。

#### How
1. 写 `/tmp/convert_test.py`：解析 converter_test.cpp，按上面的规则做文本级转换，
   保留 cork_byte helper，跳过 class TestConverter 定义，跳过 QTEST_MAIN。
2. 跑脚本生成 cork_test.cpp。
3. 手动 flatten 一处多行 `string_eq`（test_mixed 第一条，源自原文件多行 qcompare）。
4. 修首行注释 `////...` → `/****...`（脚本 bug，已就地修）。
5. 删 `tests/Data/String/converter_test.cpp`。
6. `clang-format-19 -i` 格式化新文件。
7. `g++-14 -fsyntax-only` 验证语法通过（lolly 子项目本地跑测试需要先装包，留作后续；
   测试逻辑等价于已 70/70 PASS 的 converter_test）。

#### 备注
- mogan 主项目的 `tests/**_test.cpp` glob 不覆盖 `lolly/tests/`，所以 cork_test.cpp
  目前通过 `cd lolly && xmake b lolly_tests && xmake r lolly_tests` 跑（与 herk_test
  同样）。这要求 lolly 子项目先装好 `liii-doctest` 等包。
- `tests/Data/String/` 目录现已空，保留（mogan 测试 glob 不会因为空目录报错）。
- 数据生成器入库：`lolly/lolly/data/gen_cork_data.py`。从 mogan 项目根跑
  `python3 lolly/lolly/data/gen_cork_data.py`，自动定位 `TeXmacs/langs/encoding/`，
  在脚本同目录输出 `cork_data.inc`。当 `.scm` 字典改动时，重跑该脚本即可同步表。

整理时间：2026/06/27（迁移段）
