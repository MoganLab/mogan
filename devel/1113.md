# [1113] 重新实现 utf8_to_herk 与 herk_to_utf8，移除 scm 文件依赖

## 相关文档
- [66_13.md](66_13.md) - Herk 编码的设计与转换规则
- [dddd.md](dddd.md) - 任务文档模板

## 任务相关的代码文件
- `src/Data/String/converter.hpp`
- `src/Data/String/converter.cpp`
- `tests/Data/String/converter_test.cpp`
- `lolly/lolly/data/herk.hpp`
- `lolly/lolly/data/herk.cpp`
- `lolly/tests/lolly/data/herk_test.cpp`
- `src/Scheme/L2/glue_lolly.lua`
- `src/Scheme/L3/glue_moebius.lua`

## 如何测试

### 确定性测试（单元测试）
```bash
xmake b converter_test
xmake r converter_test
xmake b lolly_tests
xmake test lolly_tests/herk_test
```

### 预期输出
```
********* Start testing of TestConverter *********
Config: ……
PASS   : TestConverter::initTestCase()
PASS   : TestConverter::test_utf8_to_cork()
PASS   : TestConverter::test_cork_to_utf8()
PASS   : TestConverter::test_utf8_to_herk()
PASS   : TestConverter::test_herk_to_utf8()
PASS   : TestConverter::cleanupTestCase()
Totals: 6 passed, 0 failed, 0 skipped, 0 blacklisted, 9ms
********* Finished testing of TestConverter *********
```

lolly 的 `herk_test` 应同样全部通过。

## 性能评估

使用临时基准测试（10000 次迭代，混合 ASCII 与 CJK/特殊字符输入）对比修改前后：

| 函数 | 修改前 | 修改后 | 加速比 |
|------|--------|--------|--------|
| `herk_to_utf8` | 12893 ms | 5196 ms | ~2.5x |
| `utf8_to_herk` | 27349 ms | 8052 ms | ~3.4x |

修改后不再加载 `TeXmacs/langs/encoding/herktounicode.scm`，转换表直接硬编码在 C++ 中；同时通过内联 UTF-8 编码减少了每次字符转换的临时对象分配。

## 如何提交

提交前执行以下最少步骤：
```bash
gf fmt
xmake b converter_test
xmake r converter_test
xmake b lolly_tests
xmake test lolly_tests/herk_test
```

## What
将 `utf8_to_herk` 与 `herk_to_utf8` 从依赖 `load_converter("...", "Hex-Cork")` 的实现，改为基于硬编码映射表的直接实现：

1. **移除 Hex-Cork 转换器加载路径**：`converter_rep::load()` 中不再加载 `herktounicode.scm`。
2. **硬编码映射表**：
   - `herk_to_utf8_code[256]`：Herk 字节到 Unicode 码点。
   - `utf8_to_herk_byte[256]`：Unicode 码点 0x00..0xFF 到 Herk 字节。
   - `utf8_to_herk_high[88][2]`：Unicode 码点 0x0100 及以上到 Herk 字节，按码点排序以便二分查找。
3. **优化实现**：
   - `herk_to_utf8` 直接按字节查表并内联输出 UTF-8 编码，避免每次调用 `encode_as_utf8` 构造临时字符串。
   - `utf8_to_herk` 解码 UTF-8 后查表，无映射时生成 `<#XXXX>` 转义。

## Why
Herk 编码是 TMU 序列化的核心编码，其转换正确性直接影响文档的读写一致性。
此前实现依赖 `TeXmacs/langs/encoding/herktounicode.scm` 文件，在打包、分发或最小化安装场景下可能缺失，导致转换器加载失败。
将映射表硬编码到 C++ 后，函数自包含、不依赖外部文件，同时消除了加载和构建 hashtree 的开销。

## How
1. 在 `src/Data/String/converter.cpp` 顶部添加三个静态映射表和一个二分查找辅助函数。
2. 重写 `utf8_to_herk` 与 `herk_to_utf8`，仅使用静态表完成转换。
3. 从 `converter_rep::load()` 中删除 `Hex-Cork` 相关分支。
4. 保留 `tests/Data/String/converter_test.cpp` 中已有的完整单元测试作为回归验证。
5. 将 `utf8_to_herk` 与 `herk_to_utf8` 的实现迁移到 `lolly/lolly/data/herk.cpp`，
   在 `lolly::data` 命名空间中提供相同签名的函数；`src/Data/String/converter.cpp`
   保留为对 `lolly::data` 的薄封装，保持现有 C++ 调用点不变。
6. 将对应的 Scheme glue（`utf8->herk`、`herk->utf8`）从 `glue_moebius.lua`
   迁移到 `glue_lolly.lua`，使其绑定到 `lolly::data::utf8_to_herk` 与
   `lolly::data::herk_to_utf8`。
7. 在 `lolly/tests/lolly/data/herk_test.cpp` 中添加针对 lolly 实现的单元测试。

## 后置任务：将 cork 编码工具迁移至 lolly

### 目标
将 `moebius/Data/String/cork.hpp` 与 `cork.cpp` 迁移到 `lolly/Data/String/`，
使 Cork/TeXmacs 字符串编解码与字符遍历等基础工具下沉到 lolly 层，供 moebius 与上层共用。

### 涉及文件
- `moebius/Data/String/cork.hpp` → `lolly/Data/String/cork.hpp`
- `moebius/Data/String/cork.cpp` → `lolly/Data/String/cork.cpp`
- `moebius/tests/Data/String/cork_test.cpp` → `lolly/tests/Data/String/cork_test.cpp`

### 改动要点
1. 使用 `git mv` 移动源文件与头文件，保持 Git 历史。
2. `cork.cpp` 依赖的 `analyze.hpp` 已在 `lolly/Kernel/Types/`，无需额外改动。
3. 源文件中均为 `#include "cork.hpp"`，移动后由 lolly 与 moebius 的公共 include 路径自动解析，
   不需要修改引用点。
4. 测试文件从 `moe_doctests.hpp` 切换到 `lolly_doctests.hpp`（`a_lolly_test.hpp`），
   并移除未使用的 `modification.hpp` 与 `tree.hpp` 包含。
5. 扩展 `cork_test.cpp`，覆盖 `tm_encode`、`tm_var_encode`、`tm_decode`、
   `tm_correct`、`tm_char_forwards/backwards/next/previous`、
   `tm_forward_access/backward_access`、`tm_string_length`、`tm_tokenize`、
   `tm_recompose`、`tm_search_forwards/backwards`、`tm_string_split`、
   `contains_unicode_char`、`downgrade_math_letters` 等全部接口。

### 验证
```bash
xmake b lolly_tests
xmake test lolly_tests/cork_test
xmake b stem
xmake b moebius_tests
```

整理时间：2026/06/26
