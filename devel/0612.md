# [0612] TikZ 插件移除 Ghostscript 依赖，改用 pdflatex + MuPDF

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板

## 2 任务相关的代码文件
- `TeXmacs/plugins/tikz/progs/init-tikz.scm`
- `TeXmacs/plugins/tikz/progs/binary/pdflatex.scm`
- `TeXmacs/plugins/tikz/goldfish/tm-tikz.scm`
- `TeXmacs/plugins/tikz/tests/tm-tikz-test.scm`
- `TeXmacs/tests/tmu/0612.tmu` — TikZ 插件非确定性测试文档

## 3 如何测试

### 3.1 确定性测试（单元测试）
1. 构建 Goldfish Scheme 解释器：
   ```bash
   xmake b goldfish
   ```
2. 运行 TikZ 插件单元测试：
   ```bash
   cd TeXmacs/plugins/tikz
   ../../../bin/goldfish load tests/tm-tikz-test.scm
   ```

### 3.2 非确定性测试（文档验证）
1. 构建 Mogan STEM：
   ```bash
   xmake b stem
   ```
2. 启动 Mogan STEM
3. 打开 `TeXmacs/tests/tmu/0612.tmu`，验证文档中已有的 TikZ 会话渲染结果正确：
   - `\draw (0,0);` 应显示提示文本而非报错
   - `\draw (0,0) -- (1,1);` 应正常显示图片
4. 手动验证：通过菜单 `Insert -> Session -> TikZ` 插入 TikZ 会话，输入 `\draw[red, line width=1pt] (0,0) -- (1,1);`，验证：
   - **语法高亮染色正常**：
     - 指令 `draw` 为蓝色
     - 操作符 `[`、`]`、`(`、`)` 和 `;` 有着色区分
     - 属性键名 `line width` 为紫色
     - 颜色常量 `red` 为森林绿色
     - 连线 `--` 为橙色
   - **括号/引号自动配对正常**：输入 `(`, `[`, `{`, `"`, `'` 会自动闭合配对，光标处于中间（注：需在编辑首选项中开启括号自动配对功能）。
   - **括号匹配自动高亮正常**：将光标移动到闭合括号旁，其对应的开始括号会被自动高亮标出。
   - **Tabstop/缩进正常**：按下 `A-tab` 应该会产生 2 个空格的缩进，`cmd S-tab` 会减少缩进。
5. 输入 `\draw (0,0) -- (1,1);`，验证是否能正常生成图片
6. 输入 `\draw (0,0);`，验证是否输出提示而不是报错或弹窗
7. 输入 `% -width 0.5par -height 0.5par` 魔术行，验证图片尺寸参数生效
8. 输入完整 LaTeX 文档（以 `\documentclass` 开头），验证直通模式

## 4 如何提交

提交前执行以下最少步骤：

```bash
xmake b stem
```

## 5 What

1. 将 TikZ 插件的图片生成流程从 `latex -> dvips -> gs -> png` 改为 `pdflatex -> pdf`
2. 利用 Mogan 内置的 MuPDF 直接渲染 PDF 图片，无需外部 Ghostscript
3. 移除 `init-tikz.scm` 中对 `(binary gs)` 的依赖
4. 移除 `tm-tikz.scm` 中的 `run-dvips`、`run-gs`、`png-size`、`eps-bbox-empty?` 函数
5. 添加 `image-valid?` 函数用于检测生成的 PDF 文件是否有效
6. 在 `binary/pdflatex.scm` 中添加 `find-binary-pdflatex` 查找逻辑
7. 更新单元测试，移除不再需要的 PNG/EPS 相关测试，保留核心逻辑测试

## 6 Why

Mogan STEM 使用 Goldfish Scheme 重写的 TikZ 插件最初依赖 Ghostscript 将 EPS 转为 PNG。这要求用户额外安装 Ghostscript，增加了环境配置负担。实际上 Mogan 内置了 MuPDF 渲染器，可以直接加载和显示 PDF 文件。因此，将插件改为直接生成 PDF 并交由 Mogan 内部渲染，可以彻底去掉 Ghostscript 依赖。

此外，原先为了处理空 bounding box 而引入的 `eps-bbox-empty?` 和 `png-size` 检测逻辑，本质上是 Ghostscript 拒绝处理极小页面时的 workaround。改用 MuPDF 后，即使页面极小也能正常渲染，因此这些 workaround 可以一并移除，代码更简洁。

## 7 How

### 7.1 改用 pdflatex 直接生成 PDF

在 `tm-tikz.scm` 中，将 `run-latex` + `run-dvips` + `run-gs` 三段式流程替换为单一的 `run-pdflatex`：

```scheme
(define (run-pdflatex tex-path pdflatex-bin)
  (let* ((inner-cmd (string-append (goldfish-quote pdflatex-bin)
                                   " --interaction=errorstopmode -halt-on-error "
                                   (goldfish-quote tex-path)
                                   " > /dev/null 2>&1"))
         (cmd (string-append "sh -c " (goldfish-quote inner-cmd)))
         (orig-dir (getcwd)))
    (unsetenv "DYLD_LIBRARY_PATH")
    (unsetenv "DYLD_FRAMEWORK_PATH")
    (unsetenv "DYLD_FALLBACK_LIBRARY_PATH")
    (unsetenv "DYLD_FALLBACK_FRAMEWORK_PATH")
    (chdir (tikz-temp-dir))
    (let ((result (os-call cmd)))
      (chdir orig-dir)
      result)))
```

`pdflatex` 直接在临时目录生成 `.pdf` 文件，参数与 `latex` 相同。

### 7.2 简化 flush-image

`flush-image` 不再需要区分 PNG/EPS，只需检查 PDF 文件是否存在且非空：

```scheme
(define (image-valid? path)
  (and (file-exists? path) (> (path-getsize path) 10)))

(define (flush-image path width height)
  (if (image-valid? path)
      (flush-file (string-append path "?" "width=" width "&" "height=" height))
      (flush-verbatim "Failed to generate image")))
```

### 7.3 更新 init-tikz.scm

移除 `(binary gs)` 模块和 `gs` 二进制参数，仅保留 `pdflatex`：

```scheme
(use-modules (binary tikz) (binary goldfish))

(define (tikz-launcher)
  (string-append
    (string-quote (url->system (find-binary-goldfish)))
    " " "load" " "
    (string-quote (string-append (url->system (get-texmacs-path))
                   "/plugins/tikz/goldfish/tm-tikz.scm"))
    " "
    (string-quote (url->system (find-binary-pdflatex)))))

(define (init-tikz)
  (plugin-configure tikz
    (:require (and (has-binary-goldfish?)
                   (has-binary-pdflatex?)))
    (:launch ,(tikz-launcher))
    (:serializer ,tikz-serialize)
    (:session "TikZ")))
```

### 7.4 更新 binary/pdflatex.scm

添加 `find-binary-pdflatex` 查找逻辑，查找路径与 `latex` 一致（macOS 上优先 `/Library/TeX/texbin/pdflatex`，Linux 上优先 `/usr/bin/pdflatex`）。

### 7.5 更新单元测试

在 `tm-tikz-test.scm` 中：
- 移除 `png-dimensions-from-bv` 和 `eps-bbox-empty?` 相关测试
- 添加 `image-valid?` 测试，验证文件存在性和大小检查
- 保留 `wrap-tikz-code` 和 `parse-magic-line` 测试

## 8 PR 描述

TikZ 插件移除 Ghostscript 依赖，改用 pdflatex 直接生成 PDF：

- 将图片生成流程从 `latex -> dvips -> gs -> png` 简化为 `pdflatex -> pdf`
- 利用 Mogan 内置的 MuPDF 直接渲染 PDF 图片，不要求用户安装 Ghostscript
- 移除 `run-dvips`、`run-gs`、`png-size`、`eps-bbox-empty?` 等不再需要的工作函数
- 添加 `image-valid?` 检测生成的 PDF 文件有效性
- 更新 `init-tikz.scm` 移除 `(binary gs)` 依赖，仅依赖 `goldfish` 和 `pdflatex`
- 在 `binary/pdflatex.scm` 中补充 `find-binary-pdflatex` 查找逻辑
- 更新单元测试覆盖新的文件有效性检测

## 9 修复默认插入大小异常

### 9.1 问题现象

切换为 PDF 输出后，默认插入的 TikZ 图片大小比之前（PNG 输出）大太多。

### 9.2 根因分析

Mogan 的插件协议处理 `flush-file` 时，`file_flush` 函数在解析 URL 的 `width=0px&height=0px` 查询参数后，对 PNG 和 SVG 有原生尺寸回退：

- PNG：`native_image_size` 读取实际像素尺寸
- SVG：`svg_image_size` 读取实际尺寸

但对 PDF 没有类似回退。当 `width == 0 && height == 0` 时，`image_flush` 默认将尺寸设为 `0.618par`，导致 PDF 图片被拉伸到接近页面宽度的 61.8%，视觉上远大于原先的 PNG。

### 9.3 修复方案

在 `tm-tikz.scm` 的 `eval-and-print` 中，当用户未指定宽高（即 `width="0px"` 且 `height="0px"`）时，将默认宽度设为 `0.3par`，高度留空由 Mogan 按比例自适应：

```scheme
(let ((final-width width)
      (final-height height))
  (when (and (string=? width "0px") (string=? height "0px"))
    (set! final-width "0.3par")
    (set! final-height ""))
  (flush-image pdf-path final-width final-height))
```

- `0.3par` 约为页面宽度的 30%，视觉上与旧流程输出的 PNG 尺寸接近
- 不读取 pdflatex 的 `.log` 文件，避免文件解析带来的复杂性和潜在卡死风险
- 此方案完全在 TikZ 插件内部实现，不修改 Mogan 核心代码

### 9.4 相关文件

- `TeXmacs/plugins/tikz/goldfish/tm-tikz.scm`

## 10 修复 \usetikzlibrary 支持

### 10.1 问题现象

当 TikZ 会话中输入以 `\usetikzlibrary` 开头的代码时，例如：

```tikz
\usetikzlibrary{shapes.geometric}
\node[draw, circle] at (0,0) {A};
\node[draw, rectangle] at (2,0) {B};
\node[draw, diamond] at (1,1.5) {C};
\draw[->] (0,0) -- (2,0);
\draw[->] (2,0) -- (1,1.5);
```

pdflatex 编译报错，输出 `pdflatex error`。

### 10.2 根因分析

`wrap-tikz-code` 函数在处理以 `\usetikzlibrary` 开头的代码时，直接将整个代码块放入 `standalone` 文档的 `document` 环境中，没有为后续的绘图命令（如 `\node`、`\draw`）包裹 `tikzpicture` 环境。这导致 pdflatex 报错，因为这些命令必须在 `tikzpicture` 环境中才能执行。

旧逻辑生成的 LaTeX：

```latex
\documentclass[tikz]{standalone}
\begin{document}
\usetikzlibrary{shapes.geometric}
\node[draw, circle] at (0,0) {A};  % 报错：不在 tikzpicture 中
...
\end{document}
```

### 10.3 修复方案

修改 `wrap-tikz-code` 函数，将 `\usetikzlibrary{...}` 命令提取到 `document` 环境的顶层，然后把剩余的绘图代码包裹在 `tikzpicture` 环境中：

```scheme
(define (wrap-tikz-code code)
  (let ((trimmed (string-trim-left code)))
    (if (string-starts? trimmed "\\documentclass")
        code
        (let* ((lines (string-split code #\newline))
               (library-lines
                 (filter (lambda (line)
                           (string-starts? (string-trim-left line) "\\usetikzlibrary"))
                         lines))
               (other-lines
                 (filter (lambda (line)
                           (not (string-starts? (string-trim-left line) "\\usetikzlibrary")))
                         lines))
               (body (string-join other-lines "\n"))
               (body-trimmed (string-trim-left body)))
          (let ((inner-code
                  (if (or (string-null? body-trimmed)
                          (string-starts? body-trimmed "\\begin{tikzpicture}"))
                      body
                      (string-append "\\begin{tikzpicture}\n" body "\n\\end{tikzpicture}"))))
            (string-append
              "\\documentclass[tikz]{standalone}\n\\begin{document}\n"
              (if (null? library-lines) "" (string-append (string-join library-lines "\n") "\n"))
              inner-code
              "\n\\end{document}"))))))
```

修复后生成的 LaTeX：

```latex
\documentclass[tikz]{standalone}
\begin{document}
\usetikzlibrary{shapes.geometric}
\begin{tikzpicture}
\node[draw, circle] at (0,0) {A};
...
\end{tikzpicture}
\end{document}
```

### 10.4 测试覆盖

在 `tm-tikz-test.scm` 中新增以下测试用例：

1. `\usetikzlibrary` + 绘图代码 -> library 在外，绘图在 tikzpicture 里
2. 多个 `\usetikzlibrary` -> 全部保留在 document 顶层
3. `\usetikzlibrary` + `\begin{tikzpicture}` -> 不重复包裹
4. 仅 `\usetikzlibrary`（无绘图代码）-> 产生空页面，由 `pdf-page-empty?` 检测

### 10.5 相关文件

- `TeXmacs/plugins/tikz/goldfish/tm-tikz.scm`
- `TeXmacs/plugins/tikz/tests/tm-tikz-test.scm`
- `TeXmacs/tests/tmu/0612.tmu`

## 11 增加 TikZ 语法高亮功能

### 11.1 任务要求

增加 TikZ 插件的语法高亮和高级编辑支持，参考 MATLAB、Python 等已有插件的语法高亮实现方式。

### 11.2 解决思路与设计

在 Mogan / TeXmacs 中，对编程语言或会话语言（通过 `prog-language` 变量控制）的语法高亮、自动括号配对和缩进支持是通过一组约定的 Scheme 模块定义的：
1. C++ 端在构建特定语言的解析器时，会执行 `prog_language(string name)` 函数。该函数中有一个底层硬性过滤条件：若 `format_exists(name)` 为假（即格式不存在），语言解析器会直接降级为 `verb_language_rep`（即不支持任何高亮的纯文本模式），从而根本不会尝试去加载 `[name]-lang.scm`。因此，我们必须为 `tikz` 定义并注册其专属数据格式。
2. C++ 端成功验证格式存在后，会自动通过 Scheme 接口加载并运行 `(code [language-name]-lang)` 模块来解析 and 绘制高亮。
3. 编辑和模式相关的逻辑可以通过定义 `(code [language-name]-mode)` 与 `(code [language-name]-edit)` 模块实现，从而定制缩进格式、快捷键和括号的自动配对匹配。

因此，我们为 TikZ 插件量身定制了数据格式与以上三个代码层面的 Scheme 模块，使其语法高亮与流畅编辑开箱即用。

### 11.3 修复/实现方案

1. **新增 `tikz.scm`** (`TeXmacs/plugins/tikz/progs/data/tikz.scm`)：
   - 注册定义 `tikz` 作为一种合法的源码格式（"TikZ source code"），使其通过 `format_exists("tikz")` 校验，打通 C++ 与 Scheme 的语言初始化链路。
   - 增加文档与代码片段双向转换器注册，保证 TikZ 代码块的完美集成。

2. **新增 `tikz-lang.scm`** (`TeXmacs/plugins/tikz/progs/code/tikz-lang.scm`)：
   - 极大地扩充了 TikZ 的关键字词库并细化了语法高亮分类：
     - **绘制核心指令** (`declare_function`): 引入了 `draw`, `node`, `path`, `fill`, `clip`, `filldraw`, `shadedraw`, `shade`, `foreach`, `usetikzlibrary`, `tikzset`, `pgfextra` 等。
     - **环境和循环控制** (`keyword_control`): 引入了 `begin`, `end`, `foreach` 等。
     - **路径绘制动作与基础形状** (`keyword`): 包含 `at`, `cycle`, `circle`, `rectangle`, `ellipse`, `arc`, `to`, `grid`, `step`, `controls`, `plot`, `coordinates`, `parabola`, `sin`, `cos`, `child`, `edge`, `svg`。
     - **扩展几何与复杂预设形状** (`constant_type`): 支持 `circle`, `rectangle`, `diamond`, `trapezium`, `semicircle`, `regular polygon`, `star`, `cylinder` 等。
     - **参数选项/样式键名** (`variable_identifier`): 注册了 `above`, `below`, `left`, `right`, `anchor`, `inner sep`, `outer sep`, `minimum height`, `minimum width`, `minimum size`, `font`, `node font`, `text width`, `align`, `line width`, `opacity`, `fill opacity`, `draw opacity`, `text opacity`, `shading`, `samples`, `domain`, `scale`, `rotate`, `shift` 等所有常用的键值参数。
     - **预置颜色名** (`constant_identifier`): 收集了 `red`, `green`, `blue`, `cyan`, `yellow`, `black`, `white`, `gray`, `darkgray`, `lightgray`, `brown`, `lime`, `olive`, `orange`, `pink`, `purple`, `teal`, `violet`, `help lines` 等标准 XColor 调色。
     - **连线与路径绘制运算符** (`operator_special`): 特别定制了高亮的 `--`, `->`, `<-`, `<->` 以增强视觉呈现。
   - 配置了丰富的语法高亮颜色偏好，将选项、指令、颜色、形状与特殊连线区分着色，呈现极佳的视觉高亮体验。

3. **新增 `tikz.ts`** (`TeXmacs/plugins/tikz/packages/code/tikz.ts`)：
   - 创建 TikZ 专属的文档样式包，声明 `tikz` 与 `tikz-code` 的文档宏。
   - 声明 `<use-module|(code tikz-edit)>` 并在创建 TikZ 会话时通过 Scheme 的 `add-style-package` 自动导入整个环境，从而对文档进行高级高亮与编辑赋能。

4. **新增 `tikz-mode.scm`** (`TeXmacs/plugins/tikz/progs/code/tikz-mode.scm`)：
   - 定义 TikZ 的激活模式谓词：`in-tikz%` 和 `in-prog-tikz%` 用于检测当前光标位置是否处于 TikZ 代码或会话编辑环境中。

5. **新增 `tikz-edit.scm`** (`TeXmacs/plugins/tikz/progs/code/tikz-edit.scm`)：
   - 定义默认制表符缩进停止位为 2 个空格。
   - 实现自动配对大括号 `{}`、中括号 `[]`、小括号 `()`、双引号 `""` 和单引号 `''`。
   - 增加光标移动时的匹配括号（Brackets）自动高亮效果。
   - 设置专属键盘映射。

6. **更新 `init-tikz.scm`** (`TeXmacs/plugins/tikz/progs/init-tikz.scm`)：
   - 使用 `lazy-format` 延迟加载 `(data tikz)` 格式。
   - 在插件加载和初始化逻辑中导入新模块 `(code tikz-mode)` 与 `(code tikz-edit)`，从而在 TikZ 插件启用时自动加载所有的编辑和语法逻辑。

### 11.4 相关文件

- `TeXmacs/plugins/tikz/progs/data/tikz.scm`
- `TeXmacs/plugins/tikz/packages/code/tikz.ts`
- `TeXmacs/plugins/tikz/progs/code/tikz-lang.scm`
- `TeXmacs/plugins/tikz/progs/code/tikz-mode.scm`
- `TeXmacs/plugins/tikz/progs/code/tikz-edit.scm`
- `TeXmacs/plugins/tikz/progs/init-tikz.scm`
