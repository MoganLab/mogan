# [1278] moganstem -c 转换默认隐含 -q

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [1277.md](1277.md) - moganstem -h 帮助信息被写入日志文件的修复

## 2 任务相关的代码文件
- src/System/Boot/init_texmacs.cpp

## 3 如何测试

### 3.1 端到端测试（手动）

```bash
xmake b stem
TEXMACS_PATH=$(pwd)/TeXmacs ./build/linux/x86_64/release/moganstem -headless 某文件.tm -c /tmp/out.tex
```

转换完成后进程应自动退出（无需显式传 `-q`），且 `/tmp/out.tex` 已生成。

显式传 `-q` 行为不变，不重复追加退出命令：

```bash
TEXMACS_PATH=$(pwd)/TeXmacs ./build/linux/x86_64/release/moganstem -headless 某文件.tm -c /tmp/out.tex -q
```

不带 `-c` 的普通启动仍然保持运行，不受影响：

```bash
TEXMACS_PATH=$(pwd)/TeXmacs ./build/linux/x86_64/release/moganstem
```

## 4 如何提交

提交前执行以下最少步骤：

```bash
gf fmt --changed-since=main
xmake b stem
```

## 5 What

`moganstem 输入 -c 输出` 转换完成后自动退出，等价于默认加上了 `-q`；
帮助信息中 `-c` 一行同步标注。

## 6 Why

`-c` 面向命令行脚本化转换，转换完成就该退出。此前必须手动补 `-q`，
否则随转换启动的 GUI 会一直挂着，批量转换时每个文件都残留一个进程。

## 7 How

`TeXmacs_main`（init_texmacs.cpp）的 `-c`/`-convert` 分支中，转换命令
（tex→tmu 特殊路径与默认 load-buffer/export-buffer 路径）追加到
`my_init_cmds` 之后，紧跟着追加 `(quit-TeXmacs)`，保证 quit 位于
`(begin ...)` 末尾、转换先于退出执行；显式传入 `-q` 时行为不变。
