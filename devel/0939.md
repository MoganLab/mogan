# 任务 0939:修复 文件→导出 菜单乱码

## 问题

用户截图反馈:Qt 客户端 文件→导出 子菜单末尾出现一条拉丁变音符乱码条目
(形如 `çşëäžÖ...`,且文本过长延伸到菜单右边缘之外),未显示原有中文内容。

## 根因

`QTMAction::set_text`(src/Plugins/Qt/QTMMenuHelper.cpp)对菜单条目标签
**无条件**按 herk 单字节表解码:

```cpp
setText (utf8_to_qstring (lolly::data::herk_to_utf8 (s)));
```

scheme 侧标签约定以 herk(`<#XXXX>` 转义,纯 ASCII)传入,此时解码正确
(词典翻译结果 `translate` 即返回 herk,所以 BibTeX/程序源码/纯文本等条目
显示正常)。但当标签是**裸 UTF-8 字节**(如含中文的拼接文本、中文文件名等)
时,每个 `>=0x80` 字节都被 herk 单字节映射表重编码成拉丁字符,显示为
变音符乱码——与截图现象完全一致。

这是 devel/1240.md 第 9 节记录的同一类 bug:当时方案是 C++ 侧
`herk_or_utf8_to_qstring`(沿用 `to_qstring` 的编码判别),但 #4421 只合入了
scheme 侧 `utf8->herk` 的针对性修正(tabpage 标题),C++ 通用修复未落地,
其余裸 UTF-8 标签路径(含 导出 菜单)依旧乱码。

## 修复(What/Why/How)

- What:补上 1240 设计的 C++ 侧通用修复,菜单标签按编码判别转换。
- Why:与其逐个在 scheme 源头 `utf8->herk`,不如让 `set_text` 同时接受
  herk 与裸 UTF-8 两种输入,任何残留的裸 UTF-8 标签都能正确显示。
- How:
  1. `src/Plugins/Qt/qt_utilities.{hpp,cpp}` 新增
     `herk_or_utf8_to_qstring`:整体是合法 UTF-8 且非纯 ASCII/universal
     时直接透传(与 `to_qstring` 同一判别),否则按 herk 解码。
  2. `QTMAction::set_text` 改调该函数;移除 QTMMenuHelper.cpp 中不再
     直接使用的 `lolly/data/herk.hpp` 包含。
  3. `tests/Plugins/Qt/qt_utilities_test.cpp` 增加回归用例
     `test_herk_or_utf8_to_qstring`:裸 UTF-8 中文透传、herk `<#..>`
     转义解码、ASCII 透传。

## 涉及文件

- `src/Plugins/Qt/qt_utilities.hpp`
- `src/Plugins/Qt/qt_utilities.cpp`
- `src/Plugins/Qt/QTMMenuHelper.cpp`
- `tests/Plugins/Qt/qt_utilities_test.cpp`

## 验证

- `xmake b qt_utilities_test && xmake r qt_utilities_test`:9 passed, 0 failed
  (含新增用例)。
- `xmake b stem`:构建通过。
- GUI 手动验证:2026-08-26 用户在 Windows 端基于本分支构建后确认
  文件→导出 子菜单乱码全部消失,「可编辑PDF...」「将选中区域导出为图像」
  等中文条目正常显示。(子菜单标题笔画偏细为原有样式,非本任务范围。)

## 调查记录

- headless 复现导出格式列表:`converters-from-special "texmacs-file" "-file"`
  过滤后为 BibTeX/Html/STEM/Source code/TeXmacs Scheme/Verbatim/docx,
  与截图中 7 条正常条目一致,乱码条目为子菜单尾部标签渲染层问题。
- 词典 `zh_CN.scm`/`english-chinese-miss.scm` 均为合法 UTF-8,无坏条目;
  `translate` 输出为 herk 转义(headless 已验证),排除词典与翻译层。
