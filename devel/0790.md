# [0790] 成批应用远端操作/应用远端操作前后保持光标位置不受改变
## What
现在 on_message 只把操作加入队列，单独成批地应用。并且在成批应用前记录 cursor 位置，之后 restore cursor，确保用户的光标位置不受影响

## 如何测试
```
xmake f --loro=yes
xmake b stem
xmake r stem
```
多个客户端同时编辑共享文档，在一个用户在一端文本的后面编辑的同时，让另一个用户敲击回车，分割一段为两段。

检查用户的光标没有被“抛出来”
