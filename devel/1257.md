# [1257] 草稿文件名用 `_` 分隔日期与时刻

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [1240.md](1240.md) - 新文档 no_name 改为 `draft_` 时间戳命名（标题规则、落盘目录）
- Issue: [mogan#4469](https://github.com/MoganLab/mogan/issues/4469)

## 2 任务相关的代码文件
- `TeXmacs/progs/texmacs/texmacs/tm-files.scm` — `scratch-buffer-name` / `draft-stamp`
- `TeXmacs/progs/texmacs/texmacs/tm-files-test.scm` — 新文件名形状、新旧标题一致、本周旧名（一个下划线）含时刻
- `TeXmacs/tests/python/1257.py` — 基于 pynput 的端到端 UI 自动化测试脚本
- `src/Texmacs/Data/new_buffer.cpp` — 未改：仍调 scheme 命名，仍按 `draft_` + `.tmu` 走标题

## 3 如何测试

### 3.1 单元测试
```bash
xmake b tm-files-test
xmake r tm-files-test
```
预期：新建名带 `YYYYMMDD_HHMMSS`；往年旧格式 `draft_20250802153000.tmu` 与新格式 `draft_20250802_153000.tmu`（含 `-1` 冲突后缀）标题相同；本周旧格式（仅 `draft_` 一个下划线）与新格式标题相同，且标题含文件名里的 `HH:MM:SS`。

### 3.2 集成测试
暂无。

### 3.3 端到端测试
端到端测试既可以手动测试，也可以采用 Python 的 pynput 框架来自动化测试：

- **自动化测试**：
```bash
python3 TeXmacs/tests/python/1257.py
```
测试流程：
1. **阶段 1：新建草稿与新命名格式校验**
   - 自动启动 MoganSTEM；
   - 自动新建标签页（Ctrl+T）；
   - 在文档中输入文本并按下 Ctrl+P 预览，触发草稿自动保存；
   - 监控草稿目录（`~/Documents/LiiiSTEM/no_name` 或系统文档对应目录）中新生成的文件；
   - 验证新生成的草稿文件名符合 `draft_YYYYMMDD_HHMMSS.tmu` 规范（即日期与时刻之间存在 `_` 分隔符）；
   - 关闭第一轮 Mogan 实例。
2. **阶段 2：存量老文件格式兼容性验证（涵盖今天、上周、去年三种用例）**
   - 将阶段 1 生成的草稿文件复制（`cp`）为老格式文件名（去掉日期与时刻之间的 `_`，生成只有 `draft_` 一个下划线的旧文件名），涵盖三种典型时间段：
     - **用例 A（今天的草稿 / 本周内）**：`draft_YYYYMMDDHHMMSS.tmu`（日期为今天），在 Mogan 打开后，验证标签页标题解析显示为周几与具体时分秒（如「草稿（周几 HH:MM:SS）」/ `Draft Weekday HH:MM:SS`）；
     - **用例 B（上周的草稿 / 今年非本周）**：`draft_YYYYMMDDHHMMSS.tmu`（日期为上周，跨周界），在 Mogan 打开后，验证标签页标题解析显示为月/日与时分（如「草稿（MM/DD HH:MM）」/ `Draft MM/DD HH:MM`）；
     - **用例 C（去年的草稿 / 往年）**：`draft_YYYYMMDDHHMMSS.tmu`（日期为去年），在 Mogan 打开后，验证标签页标题解析显示为完整年月日（如「草稿（YYYY/MM/DD）」/ `Draft YYYY/MM/DD`）；
   - 自动启动 MoganSTEM 依次打开上述三份老格式草稿文件，截屏并检查顶部标签栏区域，验证老格式草稿能正常加载且标签页标题按各自时间规则正确解析渲染（包含 `草稿` / `Draft` 时间标识，而非 fallback 成「未命名」或「no name」）；
   - 验证完毕后关闭老文件验证实例。
3. **阶段 3：清理临时测试文件**
   - 自动清理测试期间生成的新草稿及复制出的老格式草稿文件并终止进程。

- **手动测试**：
1. `xmake r stem` 启动 Mogan（Windows 下需先 `xmake i stem`）；
2. Ctrl+T 新建标签。标题仍为本周规则：`草稿（周几 HH:MM:SS）` / `Draft Weekday HH:MM:SS`，**不要**显示文件名；
3. 草稿刚创建时可能还不落盘。等自动保存或按 Ctrl+P 预览后查看 `~/Documents/LiiiSTEM/no_name/`（中文系统 `~/文档/LiiiSTEM/no_name/`）：
   文件名形如 `draft_20260901_194700.tmu`（日期与时刻之间有 `_`，可读的时间线）；
4. 同一秒快速连建两份：第二份为 `draft_YYYYMMDD_HHMMSS-1.tmu`；
5. **存量兼容（仅一个下划线的旧名，涵盖今天、上周、去年三类用例）**。标签上的人性化提示（如「草稿（周三 10:26:04）」）是从文件名里的数字推断的，不是另存字段。磁盘上已有大量 `draft_YYYYMMDDHHMMSS.tmu`（`draft_` 之后是连续 14 位，日期与时刻中间没有 `_`）。必须仍能打开并显示正确时间。

   手测步骤：
   - **今天（本周）**：把新草稿复制为 `draft_YYYYMMDDHHMMSS.tmu`（如今天 `draft_20260902102604.tmu`），打开后标签须为「草稿（周几 HH:MM:SS）」，时分秒与文件名一致；
   - **上周（今年非本周）**：把新草稿复制为上周日期的旧文件名（如 `draft_20260826131220.tmu`），打开后标签须为「草稿（08/26 13:12）」，显示月/日与时分；
   - **去年（往年）**：把新草稿复制为去年的旧文件名（如 `draft_20250902131220.tmu`），打开后标签须为「草稿（2025/09/02）」，只显示年月日；
   - 验证三类旧文件名均能正常加载，不乱码、不退化成「no name」或未命名。

## 4 如何提交

提交前执行以下最少步骤：

```bash
xmake b stem
xmake b tm-files-test && xmake r tm-files-test
python3 TeXmacs/tests/python/1257.py
gf fmt --changed-since=main
```

## 5 What

1. 新草稿文件名由 `draft_YYYYMMDDHHMMSS.tmu` 改为 `draft_YYYYMMDD_HHMMSS.tmu`。
2. 同秒冲突后缀仍是 `-N`，写在时刻之后：`draft_20260901_194700-1.tmu`。
3. `draft-stamp` 去掉 `_` 后再交给原标题切分逻辑，旧无下划线文件继续可用。
4. 增加 `TeXmacs/tests/python/1257.py` 基于 `pynput` 的端到端自动化测试脚本。
5. 标签页标题规则不改。

## 6 Why

连续 14 位数字难读。用 `_` 分开日期和时刻后，磁盘上的草稿名与标题里的时间能对上看。

## 7 How

数据流（1240 未改）：

1. C++ `make_new_buffer` 调 `(scratch-buffer-name)`，得到系统路径字符串。
2. 自动保存把内容写到该路径（目录仍是 `LiiiSTEM/no_name/`）。
3. C++ `propose_title` 见 `draft_*.tmu` 则调 `(scratch-buffer-title u)`。
4. `scratch-buffer-title` 依赖 `draft-stamp` 返回的**连续数字**，用 `substring` 切日/时/分/秒。

因此只改两处：

- **生成**：`date->string` 模板 `~Y~m~d~H~M~S` → `~Y~m~d_~H~M~S`。`scratch-candidate` 仍是 `"draft_" + stamp + ".tmu"`，stamp 里已含 `_`。
- **解析**：文件名去掉 `draft_` / `.tmu` 后，先按 `-` 丢掉冲突序号，再 `string-split` 掉 `_` 拼回数字。新名 `20260901_194700` → `20260901194700`；旧名 `20260825112126` 不变。若不剥 `_`，`substring stamp 8 10` 会切到 `_1`，本周标题坏掉。

### 端到端自动化测试实现

在 `TeXmacs/tests/python/1257.py` 中：
- 利用 `subprocess` 启动 Mogan 进程；
- 记录保存前的草稿目录文件；
- 利用 `pynput` 模拟键盘操作：Ctrl+T 新建标签页 -> 键入文字 -> Ctrl+P 预览保存；
- 检测草稿目录中新生成的文件，使用正则表达式 `^draft_(\d{8})_(\d{6})(-\d+)?\.tmu$` 校验文件名是否包含 `_` 分隔符；
- 将新生成的草稿文件 `cp` 复制为三类存量旧文件名（`draft_YYYYMMDDHHMMSS.tmu`，去掉中间的下划线）：今天（本周）、上周（今年非本周）、去年（往年）；
- 重新启动 Mogan 依次打开上述三类老草稿文件，抓取屏幕验证标签栏标题按各自时间规则正常渲染且能正确显示时间标识；
- 清理测试产生的临时新老草稿文件并终止进程。
