# [0711] 补充&调整编译宏以解耦 Qt

## What

- `renderer.cpp`: 原逻辑中，ifndef QTTEXMACS 下定义了 stub 的渲染器，现采用 mupdf。因此添加 USE_MUPDF_RENDERER 条件防止 duplicated symbol
- `research.cpp`: 调整宏逻辑，init操作不应该在 ifdef QTTEXMACS 中
- `mupdf_qt_simple_widget.cpp`: add QTTEXMACS condition
- `mupdf_qt_ui_element.cpp`: add QTTEXMACS condition
- `pdf_hummus_renderer.cpp`: add QTTEXMACS condition
- `new_window.cpp`: add QTTEXMACS condition
- `edit_interface.cpp`: add QTTEXMACS condition
- `basic_renderer.cpp`: remove QTTEXMACS condition

## Why
Qt 解耦

## 如何测试
目前可以测试 Qt 行为不改变
```
xmake b stem
xmake r stem
```
